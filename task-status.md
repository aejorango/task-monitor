# Task status — Task Monitor (v0.1)

Written by Claude Code while it works through the task sheet, generated 2026-09-20 by Ideon.
One line per finished row, appended the moment the row is done. Never edited by hand while a run is going.

Format:  <Task ID> | YES or BLOCKED | estimated AI cost in USD | one-line summary
Example: T-0001 | YES | 0.42 | Added renewal reminders; server/src/routes/reminders.ts; tests added.

Upload this file in Ideon → Tasks → "Sync from Claude Code" to mark the same rows there.
T-0001 | YES | 2.80 | users self-update is now an allowlist (displayName/photoURL/email only) so companyId cannot be self-assigned; firestore.rules, src/services/firebase.js, tests/rules/{harness,users.rules.test}.mjs, package.json (test:rules), firebase.json (emulator); 11 emulator rules tests added. Deploy with npm run deploy:rules.
T-0002 | YES | 2.60 | Invites hardened server-side: create requires project/workspace admin, get-by-id only (no list enumeration), updates limited to appending claims, claim can only set the claimers own ACL slot; presence scoped to workspaceId; getInvite switched to getDoc. firestore.rules, src/services/firebase.js, src/hooks/usePresence.js, src/components/TaskEditor.jsx, tests/rules/{invites,presence}.rules.test.mjs; 15 new emulator tests.
T-0003 | YES | 1.90 | Invite UI now mirrors the rules: sharing panel and invite listener gated on canAdministerProject, non-admins get a plain-language explanation instead of a failing button; all Firestore errors in the sharing panel and invite claim screen run through friendlyError. src/services/access.js (new, pure), src/services/access.test.mjs, src/components/ProjectsView.jsx, src/components/InviteClaimView.jsx, src/hooks/usePresence.js; 7 unit tests added.
T-0004 | YES | 0.55 | Tests and docs for BUG-002: 12 invite + 3 presence emulator tests and 7 access-rule unit tests are in place; README gained a "Security model — who can share a project" table and the npm test / test:rules / test:all commands. README.md, CHANGELOG.md. (No Implementation_plan.md exists in this repo.)
T-0005 | YES | 2.20 | Approval is now enforced in firestore.rules: isApproved() folded into every access primitive (isOwner, isCreatingOwn, workspace/project membership, isMyCompany, invite claim) plus the raw workspace/conversation/message/invite rules; users/{self} stays exempt so a new account can create and read its own profile. App.jsx gate turned into an allowlist (status !== approved waits outside). firestore.rules, src/App.jsx, tests/rules/approval.rules.test.mjs; 22 emulator tests added.
T-0006 | YES | 1.60 | spawnNextRecurrence now goes through addTask, so the next instance inherits workspaceId plus assignedTo/links/customValues/phaseId and resets subtasks+dependsOn; recurrence maths extracted to the pure src/services/recurrence.js (re-exported from firebase.js) and covered by 17 unit tests; added repairOrphanedDocuments() to adopt already-orphaned tasks/activities into the users workspace, kicked off once per device from useWorkspaces. src/services/recurrence.js, src/services/recurrence.test.mjs, src/services/firebase.js, src/hooks/useWorkspace.js. Board verification needs a signed-in Firebase session; payload shape is covered by tests.
T-0007 | YES | 2.40 | Bridge no longer lets a web page choose the binary it spawns: cliPath dropped from the HTTP-settable keys (env TM_BRIDGE_CLI_PATH or config file only), cliModel/apiModel validated against ALLOWED_CLI_MODELS, unknown and prototype keys dropped, and POST /ai/settings now needs a per-session admin code printed at startup and saved to ~/.task-monitor/bridge-token; cliPath redacted from every HTTP response. Frontend sends x-bridge-token and Settings - AI brain now has a model dropdown plus a plain-language admin-code field. bridge/ai.mjs, bridge/server.mjs, bridge/settings.test.mjs, src/services/ai.js, src/components/SettingsView.jsx, README.md; 13 unit tests added, verified live with curl.
T-0008 | YES | 1.80 | ErrorBoundary added around the whole app (main.jsx, App.jsx invite route) and around each lazy view, so a crashing page leaves the sidebar usable and shows a card with Try again / Reload and a collapsed Technical details line; navigation clears the error. Crash copy extracted to the pure src/services/errorMessages.js (stale-deploy and offline cases named specifically). Also stood up a real DOM test rig (jsdom + a rolldown JSX loader) now wired into npm test. src/components/ErrorBoundary.jsx, src/services/errorMessages.js(+test), src/App.jsx, src/main.jsx, src/App.css, dev/error-boundary.{html,jsx}, tests/ui/{dom,jsx-loader,jsx-hooks,ErrorBoundary.test}.mjs, package.json; 16 tests added.
T-0009 | YES | 2.30 | Pure logic pulled out of components and firebase.js so it can be tested: new src/services/csv.js (RFC-4180 parser, column mapping, plain-language validation, cell normalisers) and src/services/askAiCore.js (digest, intent routing, search, answer builders — no Firebase import, re-exported from askAi.js); parseQuickAdd gained an injectable `now`. 60 new unit tests across csv/nlpQuickAdd/askAiCore on top of the recurrence and rules suites. src/services/{csv,csv.test,askAiCore,askAiCore.test,askAi,nlpQuickAdd,nlpQuickAdd.test}.*
T-0010 | YES | 1.70 | CsvImporter now uses services/csv.js end to end (readActivityCsv + buildImportPreview + summarizeImport) instead of its own private copies, and the preview strip says in plain words what the import will do (rows imported, added to existing tasks, new tasks, hours, rows skipped and why) before anything is written; unreadable files explain themselves. Added tests/ui/CsvImporter.test.mjs (5 real-DOM tests) and taught the test loader to stand in for import.meta.env. src/components/CsvImporter.jsx, src/services/csv.js(+test), tests/ui/{CsvImporter.test,dom,jsx-hooks}.mjs; 16 tests added.
T-0011 | YES | 0.60 | Tests and docs for IMP-005: npm test now runs 177 tests across 12 suites (bridge, due alerts, recurrence, csv, quick-add, askAiCore, access, error messages, and real-DOM component tests) and npm run test:rules runs 48 emulator tests; README gained a Tests section with a suite-by-suite table and a "where logic lives" rule, CLAUDE.md gained the same rule under Conventions plus the new commands. README.md, CLAUDE.md, CHANGELOG.md. (No Implementation_plan.md in this repo.)
T-0012 | YES | 0.90 | Settings default project is now honoured by the Board quick-add: the preselected project is derived during render (filter > saved default > first project) via the new pure src/services/preferences.js, so a deleted default falls back instead of leaving the box blank and the dropdown never flashes empty. src/components/TaskForm.jsx, src/services/preferences.js(+test), tests/ui/TaskForm.test.mjs; 11 tests added.
T-0013 | YES | 0.70 | Shared-only projects now carry _shared: true, so the "Shared" badge AppShell and ProjectsView already render can actually appear; merge logic extracted to the pure src/services/projects.js (shared projects from other workspaces still never bleed in, and the source object is not mutated). src/hooks/useTasks.js, src/services/projects.js(+test); 7 unit tests added.
T-0014 | YES | 0.80 | Added src/services/download.js as the single door for handing a file to the user: stampedName() produces <name>-YYYY-MM-DD.<ext> from todayLocal() (never the UTC day, which stamped Manila mornings with yesterday), safeFileName() sanitises project names, mimeFor() covers csv/json/md/txt/html/pdf/docx/xlsx/pptx, and downloadFile() does the blob-and-anchor dance once. src/services/download.js(+test); 7 unit tests added.
T-0015 | YES | 1.10 | All five export buttons (Activity Log CSV, Projects log CSV, Settings JSON backup, WBS modal CSV, WBS view CSV) now call downloadFile(), so every file is <name>-YYYY-MM-DD.<ext> in the users local day and project names are sanitised once. Two regression guards added: no source file may build a filename from toISOString(), and no file outside services/download.js may set a.download. src/components/{TableView,SettingsView,ProjectsView,WbsModal,WBSView}.jsx, tests/ui/download.test.mjs; 7 tests added.
T-0016 | YES | 0.35 | Tests and docs for BUG-007: 14 tests cover the stamped-filename helper and the two regression guards; README gained the "every downloadable file is date-stamped" rule and the new suite rows, CLAUDE.md gained the same rule under Conventions and a matching Common Pitfall. README.md, CLAUDE.md, CHANGELOG.md.
T-0017 | YES | 1.20 | Added a distinct bridge-api provider so a bridge running on an ANTHROPIC_API_KEY is no longer reported as claude-code: detectProvider now returns bridge-api in both the explicit and auto paths, providerLabel/providerHeadline say "billed per token" instead of "no API billing", and new canBrowse()/canGround()/isBridgeProvider() helpers mean a web or notebook-grounded request through an API-mode bridge comes back degraded instead of silently ungrounded. src/services/ai.js, src/services/aiProvider.test.mjs; 8 unit tests added.
T-0018 | YES | 1.00 | Settings - AI brain now takes its headline from providerHeadline() in services/ai.js instead of writing its own, so a bridge on an API key reads "billed per token" and says how to get onto the subscription; the non-admin AI access card reads useAiStatus().available instead of profile.companyId, so bridge users with no company are no longer told AI is unavailable while every sparkle button works, and mock mode is labelled as placeholder rather than enabled. src/components/SettingsView.jsx, tests/ui/AiBrainPanel.test.mjs; 5 tests added.
T-0019 | YES | 0.30 | Tests and docs for BUG-008: 13 tests cover the bridge-api provider, its label/headline, its capability gates and the non-admin card; CLAUDE.md gained a "provider is not transport" rule plus two Common Pitfalls (do not gate AI on companyId; do not treat bridge-api as claude-code), README gained the new suite row. CLAUDE.md, README.md, CHANGELOG.md.
T-0020 | YES | 0.60 | Sidebar user block now subscribes with onAuthChange instead of polling auth.currentUser every 500 ms (the poll re-rendered the sidebar twice a second forever and could not even detect what it was watching, since Firebase mutates currentUser in place); name, photo and email now prefer the live Firestore profile so an edit shows immediately. src/components/AppShell.jsx, tests/ui/SidebarUserBlock.test.mjs; 3 tests added.
T-0021 | YES | 1.40 | Repo docs brought back in line with the app: README.md rewritten as a real front door (run locally, Firebase config and the six env vars, first-superadmin approval flow, what every page does, workspaces and roles, tech, deploy, docs map, project structure, dev harnesses), docs/BUILD-GUIDE.md and docs/package-scripts.md moved to docs/archive/ with a note saying why they are wrong, and docs/firestore-schema.md marked as shape-only with its anonymous-auth and "multi-user is future work" passages corrected. README.md, docs/archive/*, docs/firestore-schema.md.
T-0022 | YES | 1.30 | In-app copy no longer speaks to developers: the Webhooks panel says plainly that nothing is delivered yet instead of naming FEATURE_ROADMAP.md and a Cloud Function, all 29 "Check console." error messages became plain sentences routed through friendlyError(err, ...), and a failed CSV import now lists the line number, task and reason per failed row instead of "see console for details". Added tests/ui/copy.test.mjs, which fails the build if any screen names a repo file or tells a user to open the console. 14 components, src/services/firebase.js, tests/ui/copy.test.mjs; 3 guards added.
T-0023 | YES | 0.30 | Tests and docs for IMP-006: tests/ui/copy.test.mjs guards the three copy rules (no repo filenames, no "open the console", webhooks honest about delivery); CLAUDE.md gained a Common Pitfall pointing at friendlyError and that guard, README gained the suite row. The archived docs carry their own README explaining why they are wrong. CLAUDE.md, README.md, CHANGELOG.md.
T-0024 | YES | 1.60 | One install, one run: added npm start (scripts/start.mjs, zero-dependency launcher that runs vite and the AI bridge together with prefixed output, treats the bridge as optional and takes both down on Ctrl-C), plus a config guard — main.jsx now calls readFirebaseConfig(import.meta.env) before importing anything that touches Firebase and renders SetupRequiredView naming each missing or still-example variable instead of dying inside the SDK with a white page. package.json, scripts/start.mjs, src/main.jsx, src/components/SetupRequiredView.jsx, src/services/firebaseConfig.js(+test), src/App.css, README.md, CLAUDE.md, tests/ui/SetupRequiredView.test.mjs; 15 tests added; verified by running npm start with .env removed.
T-0025 | YES | 0.70 | The browser-notification "already shown" set is now pruned on every read: pruneShownKeys() in services/dueAlerts.js drops keys whose due date is more than 30 days old plus anything malformed, keeps future dates so an early announcement never repeats, and the pruned set is written back on every scan including one with nothing due (saving only on a new notification is how it grew forever). src/services/dueAlerts.js(+test), src/hooks/useNotifications.js; 9 unit tests added.
T-0026 | YES | 1.00 | Added the pure src/services/knowledgeCopy.js: knowledgeState() collapses the probe result into one of seven states, and knowledgeCopy(status, {isOperator}) returns either the operators runbook (title, message and the one shell command that moves that state forward) or a plain sentence for everyone else — no commands, no bridge URL, and the same wording for every unavailable reason so the internal cause is not leaked through five phrasings. src/services/knowledgeCopy.js(+test); 9 unit tests added.
T-0027 | YES | 1.20 | Settings - Knowledge base is now told who is reading it: KnowledgeSection takes isOperator (true only for approved superadmins) and renders either the operator runbook or a single card saying whether a knowledge base is connected and that the admin sets it up — no shell commands, no bridge URL, no Re-check button for anyone else. The hardcoded INSTALL_CMDS list is gone; every command now comes from knowledgeCopy(). src/components/KnowledgeSection.jsx, src/components/SettingsView.jsx, tests/ui/KnowledgeSection.test.mjs; 6 tests added.
T-0028 | YES | 0.30 | Tests and docs for POL-003: 15 tests cover the two audiences (9 on the copy module, 6 driving the real section in a DOM) and assert that no shell command, CLI name or bridge URL can reach a non-operator; CLAUDE.md gained a matching Common Pitfall, README notes the section is superadmin-only and lists the new suite. CLAUDE.md, README.md, CHANGELOG.md.
T-0029 | YES | 0.90 | Added the pure src/services/approvalCopy.js: approvalCopy(profile) returns the right title, message and wait note for setting-up / pending / rejected — telling a pending user the honest thing (no email is sent, but the page lets them in the moment someone approves) instead of promising a notification nothing can send; sessionLine(profile, authUser) replaces "Anonymous session <uid>"; SETTINGS_SUBTITLE describes what the page really holds. src/services/approvalCopy.js(+test); 9 unit tests added, including a guard that no state promises an email.
T-0030 | YES | 1.00 | The three screens now say true things: the waiting screen and the sign-in fine print no longer promise an approval email (there is no backend to send one) and instead say the page lets you in the moment someone approves — which the live profile subscription already does; a declined account is no longer told to keep waiting, and a profile still arriving is no longer rendered as a rejection; Settings says "Signed in as <name> (<email>)" instead of "Anonymous session <uid>" and its subtitle describes what the page actually holds. src/components/{PendingApprovalView,LandingView,SettingsView}.jsx, tests/ui/approvalScreens.test.mjs; 9 tests added.
T-0031 | YES | 0.30 | Tests and docs for POL-004: 18 tests cover the approval copy (9 pure, 9 driving the real screens), including a repo-wide guard that no component may promise an approval email; CLAUDE.md gained two Common Pitfalls (do not promise what the app cannot do; the session is not anonymous), README lists the new suite. CLAUDE.md, README.md, CHANGELOG.md.
T-0032 | YES | 0.90 | Version and deploy account are no longer hardcoded: package.json is 0.2.0, vite.config.js bakes it (and the build date) in as __APP_VERSION__/__BUILD_DATE__ so Settings - About and the sidebar footer always show the build that actually shipped, and the three deploy scripts now go through scripts/firebase-deploy.mjs which reads FIREBASE_ACCOUNT instead of a personal Gmail address, also covers storage:rules, and explains a missing CLI. package.json, vite.config.js, scripts/firebase-deploy.mjs, src/services/appVersion.js(+test), src/components/{SettingsView,AppShell}.jsx, src/App.css, README.md; 6 tests added.
T-0033 | YES | 1.80 | Board no longer opens a listener per card: added the ref-counted src/services/sharedSubscription.js and routed the auth state, workspaces, tasks, projects and workspace-activities subscriptions through it, so one Firestore query serves any number of components and useActivities(taskId) now derives from the single workspace-wide activities listener instead of opening its own and filtering client-side; late subscribers get the current value immediately instead of a blank frame. src/services/sharedSubscription.js(+test), src/hooks/{useWorkspace,useTasks}.js, tests/ui/sharedListeners.test.mjs; 17 tests added. Also de-flaked bridge/settings.test.mjs (a 1-in-16 token collision).
T-0034 | YES | 3.20 | The company Anthropic key no longer reaches any browser. It moved from the company document (readable by every member) to companies/{id}/secrets/anthropic, which firestore.rules opens only to superadmins; a new Cloud Function (functions/index.js, aiProxy) holds it server-side, checks the caller (approved + company + aiEnabled + key present) via the pure functions/src/authorize.js, clamps the token budget, forwards to Anthropic, never echoes the upstream body, and writes an aiUsage record a member cannot forge. The client gained a proxy provider and src/services/aiProxyClient.js that sends only the prompt; aiCredentials no longer stores a company key at all and getEffectiveApiKey() returns only a superadmins own device key; updateCompany writes the secret and records hasApiKey, with migrateCompanyKeys() to move existing keys and delete the field. NOTE FOR REVIEW: deploying the function needs the Firebase Blaze plan, and CLAUDE.md currently lists a backend as out of scope — that line needs your decision. functions/**, firestore.rules, src/services/{ai,aiCredentials,aiProxyClient,firebase}.js, src/hooks/useCompany.js, firebase.json, package.json, scripts/firebase-deploy.mjs; 30 tests added (14 function, 8 client, 8 emulator).
T-0035 | YES | 1.40 | Settings - Companies no longer renders a stored key: the API-key box starts empty and means "replace it with this" (blank leaves the existing key alone rather than wiping it), key presence comes from a hasApiKey flag, the model became a dropdown instead of a free-text box that a typo could break, the copy now says the key is held on the server and members never receive it, and opening the panel runs the one-off migration that moves any key still on a company document into the secret and deletes it. User management and the AI-access card read the flag too. src/components/SettingsView.jsx, tests/ui/companyKey.test.mjs; 8 tests added.
T-0036 | YES | 0.40 | Tests and docs for IMP-001: 30 tests across the function authorizer, the client and the emulator, including guards that the browser never holds a company key and the built bundle contains no sk-ant string; CLAUDE.md gained the AI-proxy entry, the "a company key never reaches a browser" rule, a rewritten Out-of-Scope line explaining that functions/ is the single deliberate exception, and a Common Pitfall; README documents the five providers, the proxy and npm run deploy:functions. CLAUDE.md, README.md, CHANGELOG.md.
T-0037 | YES | 1.50 | Activity queries are now bounded by Firestore instead of by the browser: subscribeToAllActivities and subscribeToRecentActivities use orderBy(date desc) + limit (and a server-side date bound for the 30-day window) rather than downloading the whole collection and slicing it, and a new one-shot loadMoreActivities(workspaceId, beforeDate) pages older history with startAfter plus an extra row so the caller knows whether to offer "Load more". Added firestore.indexes.json with the eight composite indexes those queries need, wired into firebase.json and npm run deploy:indexes. src/services/firebase.js, firestore.indexes.json, firebase.json, package.json, scripts/firebase-deploy.mjs, src/services/firestoreQueries.test.mjs; 9 tests added.
T-0038 | YES | 1.30 | useAllActivities now pages: the live listener holds one page of the newest entries and loadMore() appends older ones with a one-shot read, hasMore is false unless a full page came back (so "Load more" is never offered at the end), switching workspace drops the fetched pages during render, and the merged list stays newest-first. Activity table and Work Performed each gained a "Load older…" row that explains in plain words why history is not all on screen. src/hooks/useTasks.js, src/components/{TableView,WorkPerformedView}.jsx, src/App.css, tests/ui/activityPaging.test.mjs; 10 tests added.
T-0039 | YES | 0.35 | Tests and docs for IMP-003: 19 tests cover the bounded queries, the index file and the paging UI; CLAUDE.md gained "bound every query at the server" as rule 5, a rewritten composite-index rule pointing at firestore.indexes.json, and a matching Common Pitfall; README documents npm run deploy:indexes and the two new suites. CLAUDE.md, README.md, CHANGELOG.md.
T-0040 | YES | 2.10 | Members can be invited by email instead of by raw Firebase UID, with no backend: an admin records the invitation on the workspace (pendingInvites for the record, pendingInviteEmails so array-contains can find it, pendingInviteRoles so the security rule can check the role) and the invited person joins themselves at sign-in and on app load via claimPendingWorkspaceInvites. The new isClaimingWorkspaceInvite() rule lets them add exactly themselves, at exactly the role offered, only if the invitation names their address, and only if the invitation is consumed — they cannot promote themselves, bring anyone else, or touch the workspace on the way in. src/services/invites.js(+test), src/services/firebase.js, src/hooks/useWorkspace.js, firestore.rules, tests/rules/workspaceInvite.rules.test.mjs; 26 tests added (15 unit, 11 emulator).
T-0041 | YES | 1.60 | The workspace members screen now speaks in names and emails: the primary control is "Invite someone by email" with a role dropdown and inline validation, pending invitations are listed with who they are for and what role they join at plus a withdraw button, member rows use memberLabel/memberSubLabel (name, then email, never a raw UID), the Account ID box moved behind an Advanced toggle visible only to superadmins, and the screen says plainly that no email is sent so nobody waits for one. src/components/SettingsView.jsx, tests/ui/workspaceMembers.test.mjs; 10 tests added.
T-0042 | YES | 0.35 | Tests and docs for MISS-001: 36 tests cover the invite rules, the claim (11 emulator cases including the role-escalation attempt that first got through) and the UI; CLAUDE.md gained the three new workspace fields in the data model, moved invite-by-email out of Out-of-Scope into shipped, and gained two Common Pitfalls (never ask for a UID; never write an invite field without its flat twin); README documents the flow and the two new permission rows. CLAUDE.md, README.md, CHANGELOG.md.
T-0043 | YES | 2.40 | Webhooks are delivered for real. Two Firestore triggers (functions/webhooks.js, onTaskWritten and onActivityWritten) work out what happened via the pure functions/src/webhookEvents.js, fan out to the workspaces matching hooks, sign the exact body with HMAC-SHA256 under each hooks own secret (x-taskmonitor-signature), retry only transient failures on a 0/30/300s backoff, and write every attempt to webhookDeliveries with a sentence an admin can act on. Guards: https only, no loopback or private-network addresses, a fixed payload shape rather than the whole document, and counters the app bumps do not fire task.updated. Added rules for the read-only delivery log, two composite indexes, and subscribeToWebhookDeliveries. functions/{index,webhooks}.js, functions/src/webhookEvents.js(+2 test files), firestore.rules, firestore.indexes.json, src/services/firebase.js; 25 tests added.
T-0044 | YES | 1.20 | Settings - Webhooks now reports reality: the "Not sending yet" warning is gone, the copy explains in plain words that each message is signed with your secret so the other tool can check it, every webhook row shows whether its last attempt was delivered or failed, and a "Delivery history" panel lists the last 50 attempts (event, target, the outcome sentence the function wrote, retry count and time) read from the read-only webhookDeliveries log, subscribed only while the panel is open. Also fixed an undefined isSuperadmin in WorkspacesSection found by lint. src/components/SettingsView.jsx, src/App.css, tests/ui/webhookDelivery.test.mjs; 11 tests added.
T-0045 | YES | 0.40 | Tests and docs for MISS-002: 36 tests cover event selection, payload shape, URL safety, retry policy, the HMAC and the UI; CLAUDE.md gained the webhook functions in the AI/provider map, moved webhook delivery out of Out-of-Scope into shipped, and gained a Common Pitfall about firing task.updated for app-written counters; README gained a Webhooks section explaining the signature, the retry policy and the delivery log in terms a receiver implementer can act on. CLAUDE.md, README.md, CHANGELOG.md.
T-0046 | YES | 2.60 | Added src/services/exporters.js: one document model (heading/paragraph/bullets/table/keyValues) rendered to seven formats — .xlsx (exceljs, bold frozen header, autofilter, sized columns), .docx (docx), .pdf (jspdf + autotable, real pagination), .md, .html (self-contained, escaped), .txt and .csv — with the three heavy libraries behind dynamic imports so they cost nothing until someone exports, and every file named through downloadFile so it is date-stamped. Verified by decoding the actual bytes: the xlsx is loaded back with exceljs and its cells checked, the docx is confirmed to be a zip containing [Content_Types].xml and word/document.xml, the pdf starts %PDF- and a 200-row table spans several pages. src/services/exporters.js(+test), tests/ui/exporters.test.mjs, package.json; 27 tests added.
T-0047 | YES | 2.30 | Export is now a first-class control on the pages that produce something worth keeping: a reusable ExportButton (format menu, aria menu semantics, click-outside/Escape, spinner, and the saved filename afterwards) wired into Review (full status report with hours by project, completed, overdue, blockers — plus three Excel sheets), Board/Gantt (the on-screen task list with filters applied, grouped by project for reading and flat for Excel, via the new src/services/taskExport.js), Minutes (attendees, notes, decisions, action-item table, points raised — src/services/minutesExport.js) and Goals (initiative, KPI, change agenda, deliverables with averaged project progress). build() runs only when a format is picked, and only deliberate export errors are shown to the user. The JSON dump is relabelled "Backup everything (.json)". src/components/ExportButton.jsx, src/services/{taskExport,minutesExport,exporters}.js(+tests), src/components/{ReviewView,Board,MinutesView,GoalsView,SettingsView}.jsx, src/App.css, tests/ui/exportButtons.test.mjs; 37 tests added.
T-0048 | YES | 0.40 | Tests and docs for MISS-003: 47 tests cover the document model, the three builders and the button, plus ten that decode the produced .xlsx/.docx/.pdf bytes to prove they are valid files; CLAUDE.md gained an Exports section (the module map, the dynamic-import rule with the actual chunk sizes, the downloadFile rule and the exportError rule) plus a matching Common Pitfall; README gained a "Getting work out of the app" table of every page, what it produces and in which formats. CLAUDE.md, README.md, CHANGELOG.md.
T-0049 | YES | 1.80 | Saved views can now remember a table, not just filters: new src/services/tableViews.js defines 13 task columns (each with its own display text and a sort value, so Status reads To do -> In progress -> Done rather than alphabetically), six grouping options, and pure sortTasks/groupTasks/rowCells/headerCells/toggleSort; normalizeTableConfig makes any stored config renderable (unknown columns dropped, the title never hideable, blanks sorting last in both directions) and tableConfigFields keeps only the four fields that belong in Firestore. addSavedView persists columns and groupBy alongside the existing sort. src/services/tableViews.js(+test), src/services/firebase.js; 24 unit tests added.
T-0050 | YES | 2.00 | New Reports - Task table: every task as a report you shape yourself — a column picker (tick to show, arrows to reorder, title locked on, reset to default), group-by project/phase/status/priority/assignee with counted headings in meaningful order, click-any-header sorting with aria-sort, click a row to open the task, Export to spreadsheet/CSV/PDF, and "Save as view" / "Update this view" which persists only the four table fields. Opening a saved view restores its exact columns, grouping and sort; switching between two swaps the table during render. src/components/TasksTableView.jsx, src/App.jsx, src/components/AppShell.jsx, src/App.css, tests/ui/tasksTable.test.mjs; 12 tests added.
T-0051 | YES | 0.35 | Tests and docs for NEW-001: 36 tests cover the config round trip (a saved view reopens with the same columns, grouping and sort), the meaning-order sorting, grouping and rendering, and the rendered page; CLAUDE.md gained the Task table to the view list and the extended savedViews schema, README lists the page and its export formats and the new suite. CLAUDE.md, README.md, CHANGELOG.md.
T-0052 | YES | 0.90 | Long notebook text sources no longer blow past ARG_MAX: the new pure sourceAddPlan() keeps short notes inline as before and switches anything over the stdin threshold to a temp file added with --type file, under a readable sanitised filename; addSourceText writes and removes that file in a finally so nothing is left behind whether the CLI succeeds or fails. bridge/notebooklm.mjs(+test), CLAUDE.md; 7 fixture tests added, including a real 300 KB round trip that asserts the byte count arrived and the temp file is gone.
T-0053 | YES | 0.80 | Minutes can now leave the app three ways: Export (Word/PDF/Markdown/web/text, landed in T-0047), "Copy as text" which puts the same document on the clipboard as Markdown ready to paste into Slack or email, and "Print" which flags just that card so the print stylesheet hides the sidebar, topbar and every other minute — giving a clean printout, and a clean PDF from the print dialog. Both buttons stop the click from collapsing the card, and the print state is cleaned up on afterprint with a Safari fallback. src/components/MinutesView.jsx, src/App.css, tests/ui/minutesShare.test.mjs; 7 tests added.
T-0054 | YES | 1.00 | Task and project data can leave as more than JSON: the Gantt page gained an Export that hands over exactly the scheduled tasks on the chart (project filter and period window applied) as .xlsx/.csv/.pdf, matching the Board export added in T-0047; the JSON dump now covers workspaces, projects, tasks, activities, goals, minutes and templates rather than three collections, carries a format version and a per-collection count so a restore can be checked, and is labelled "Backup everything (.json)". src/components/GanttView.jsx, src/components/SettingsView.jsx, tests/ui/dataExport.test.mjs; 7 tests added.
T-0055 | YES | 1.90 | ⌘K is now a command palette, not only a search box: type "new project Website revamp", "log hours", "create meeting notes" or just "gantt" and the matching action appears, with a New-task row previewing exactly what the natural-language parts will do (title, due date, priority, tags, requested-by) before you commit. Commands lead when you are clearly issuing one and trail search results otherwise, arrow keys cover every row, and running one navigates to the page that owns that thing and opens its create flow prefilled via the single-source useQuickCreate hook. Board, Projects, Minutes and Goals all listen. src/services/commandPalette.js(+test), src/hooks/useQuickCreate.js, src/components/{AppShell,Board,TaskForm,ProjectsView,MinutesView,GoalsView}.jsx, src/App.css, tests/ui/commandPalette.test.mjs; 26 tests added.
T-0056 | YES | 1.20 | Projects and segments now carry an icon, not just a colour: new src/services/icons.js holds one 24-mark monochrome set with normalizeIcon (nothing outside the set is ever stored or rendered), iconFor (own icon, then the parents, then the default) and suggestIcon (a stable per-id mark so an existing project is never blank). The project editor gained an icon picker beside the colour swatches, addProject/updateProject persist it, addSegmentToWorkspace accepts one, and project cards and the project header render the icon in the projects own colour, marked aria-hidden so screen readers read the name. src/services/icons.js(+test), src/components/ProjectsView.jsx, src/services/firebase.js, src/App.css, tests/ui/groupIcons.test.mjs; 18 tests added.
T-0057 | YES | 1.50 | Soft-deleted items are visible and restorable at last: new Trash view in the sidebar lists deleted tasks, projects, minutes and goals with when each went, a Restore button that is the exact inverse of the soft delete, and an inline two-step "Delete forever" for the three kinds that can be purged. Tasks deliberately cannot be purged — activities reference them by id — and the row says so rather than hiding the button. Backed by subscribeToDeleted / restoreDeleted / deleteForever, which is now the only hard delete in the app. src/components/TrashView.jsx, src/services/firebase.js, src/App.jsx, src/components/AppShell.jsx, tests/ui/trash.test.mjs; 11 tests added.
T-0058 | YES | 1.70 | New Reports - Timesheet: people down the side, the seven days of the chosen week across the top, totals on both edges plus a "where the hours went" project split. Week navigation (previous/next/this week) honours the Settings week-start preference, a project dropdown filters the whole grid and its totals together, and Export produces a two-sheet workbook (the grid with a totals row, and the project split) named timesheet-<week-start>-YYYY-MM-DD. Arithmetic lives in the pure src/services/timesheet.js — deleted entries and other weeks excluded, people named never shown as uids, rounding done once at the end so fractions do not drift. src/services/timesheet.js(+test), src/components/TimesheetView.jsx, src/App.jsx, src/components/AppShell.jsx, src/services/commandPalette.js, src/App.css, tests/ui/timesheet.test.mjs; 29 tests added.
T-0059 | YES | 2.10 | New four-step import wizard that takes any spreadsheet: choose what you are importing (tasks, projects or the activity log), pick the .csv, map each of our fields onto one of YOUR columns from a dropdown (guessed first from the headings — "Summary", "Deadline", "Importance" all land correctly), then read a preview with line numbers and a per-row reason before anything is written. Status and priority understand how people actually write them, multi-value cells split on commas/semicolons/newlines, a blank date is left blank rather than silently becoming today, missing projects are created rather than dropped, writes are batched at 400, and failed rows come back by line with a reason. Offered from the Activity Log and the Task table. src/services/csv.js(+test), src/components/ImportWizard.jsx, src/components/{TableView,TasksTableView}.jsx, src/App.css, tests/ui/importWizard.test.mjs; 26 tests added.
T-0060 | YES | 1.60 | Process template gallery: six ready-made processes (client project, product launch, audit or review, event, new joiner, start from scratch) shipped as plain data in src/templates/gallery.js, each with its phases, a starting set of real task titles, a one-line summary and who it is for. Picking one shows the phases you will get and prefills the name; creating it makes the project and every task in the right phase. Offered from the Projects header and, on a blank account, as the leading call to action instead of "click + New project". src/templates/gallery.js(+test), src/components/TemplateGallery.jsx, src/components/ProjectsView.jsx, src/App.css, package.json, tests/ui/templateGallery.test.mjs; 21 tests added.
T-0061 | YES | 1.80 | Undo toast, on top of the Trash view from T-0057: new ToastProvider (mounted once in App.jsx) replaces window.alert for anything that is not a question — stacking, auto-dismissing, pausing on hover, role=alert for errors and role=status for confirmations, a Dismiss button, and an Undo button when there is something to take back (shown for longer, because you have to notice it first). Deleting a task, project or minute no longer stops to ask: it deletes, says so, and offers Undo that calls restoreDeleted; an undo that fails says so rather than silently doing nothing. src/components/Toast.jsx, src/App.jsx, src/App.css, src/components/{TaskEditor,ProjectsView,MinutesView}.jsx, tests/ui/toast.test.mjs; 15 tests added.
T-0062 | YES | 1.10 | Command palette completed: on top of the create and navigate actions from T-0055, ⌘K now opens with "Recently opened" instead of a blank panel — the last six things you looked at, recorded per device, deduped and bounded, surviving corrupt storage or no storage at all. Picking one reopens it (a task in its own project, a project on its board). Navigation commands for every view were already in place. src/services/commandPalette.js(+test), src/components/AppShell.jsx, tests/ui/commandPalette.test.mjs; 14 tests added.
T-0063 | YES | 1.40 | Dashboard gained "Status report": a portfolio document in PDF, Word, Markdown or web-page form with the headline numbers, a red/amber/green table of every project (worst first, each colour explained in a sentence — "3 tasks overdue, 2 blocked entries, 45% behind its schedule" — and the colour key printed so the table stands alone), the overdue list with owners, the blocker list, and the AI narrative when one has been generated, always attributed so nobody mistakes prose for a computed number. Built on demand from the same digest the app shows, so the two can never disagree; also produces a two-sheet workbook. src/services/statusReport.js(+test), src/components/DashboardView.jsx, tests/ui/statusReport.test.mjs; 19 tests added.
T-0064 | YES | 1.60 | Dialog primitives to replace window.confirm/prompt: new src/components/Dialog.jsx with a Modal shell (role=dialog, aria-modal, aria-labelledby from a generated id, focus moved in on open and restored on close, a real Tab trap in both directions, Escape to close, backdrop click only on the backdrop itself), ConfirmDialog and PromptDialog on top of it, and a useDialog() hook whose confirm()/prompt() return promises so a call site reads like the confirm() it replaces. Falls back to the browsers own dialogs when no provider is mounted, so a promise can never hang. Mounted once in App.jsx alongside ToastProvider. src/components/Dialog.jsx, src/App.jsx, src/App.css, tests/ui/{dialog.test,dom}.mjs; 12 tests added.
T-0065 | YES | 2.80 | All 94 native dialogs replaced: 66 alert() calls became toast.error/success, 25 confirm() calls became await ask.confirm({ title, message, confirmLabel, danger }) and 3 prompt() calls became await ask.prompt(...), with every enclosing handler made async, useToast/useDialog wired into the 21 components that needed them, destructive confirms labelled with the actual verb (Delete/Remove/Revoke) and marked danger, long strings split into a question plus an explanation instead of a title full of newlines, and every toast.error(err.message) routed through friendlyError so no raw SDK text reaches a user. tests/ui/noNativeDialogs.test.mjs now fails the build if alert/confirm/prompt reappears, if a confirm lacks a title or an action label, if a destructive one is not marked, or if a toast shows a raw SDK message. 21 components + tests/ui/noNativeDialogs.test.mjs; 7 guards added.
T-0066 | YES | 0.35 | Tests and docs for POL-001: 19 tests cover the toast and dialog primitives plus seven repo-wide guards (no native dialogs anywhere, the one documented fallback, every confirm has a title and a real action label, destructive ones are marked, no explanation crammed into a title, no raw SDK message in a toast, both providers mounted once); CLAUDE.md gained a "Telling the user something" section and two Common Pitfalls, README lists the two new suites. CLAUDE.md, README.md, CHANGELOG.md.
T-0067 | YES | 1.30 | Added src/hooks/useModalDialog.js: gives an existing modal real dialog behaviour in two lines rather than a rewrite — spread backdropProps on the backdrop and dialogProps on the panel and it gains role=dialog, aria-modal, aria-labelledby (or aria-label when there is no title element), focus moved in on open and restored to wherever it came from on close, a Tab trap that wraps at both ends and recaptures focus that escapes, Escape to close with stopPropagation so a nested modal closes only itself, and a backdrop click that fires only on the backdrop itself. closeOnEscape and closeOnBackdrop can be turned off for a modal mid-save. src/hooks/useModalDialog.js, tests/ui/useModalDialog.test.mjs; 11 tests added.
T-0068 | YES | 2.30 | All 23 remaining modals adopted useModalDialog: each backdrop and panel now spreads the hooks props, so every one has role=dialog, aria-modal, a real aria-labelledby (each modals own heading was given the generated id; the two full-bleed editors with a breadcrumb hero instead of a heading got an explicit title), focus moved in on open and restored on close, a Tab trap, Escape to close, and a backdrop click that only fires on the backdrop. The CSV importer and the import wizard keep their "not while writing" guard. DueTaskAlertModal was deliberately left alone — it is role=alertdialog with its own describedby and ref, which the hook would have overridden. 21 components; tests/ui/modalSemantics.test.mjs guards all of it (9 checks).
T-0069 | YES | 0.80 | Tests and docs for POL-002, plus the part of it still outstanding: 27 icon-only buttons (✕ ✎ ↑ ↓ × across 10 components) gained an aria-label, reusing the title already written for a mouse user so the two never contradict each other. tests/ui/modalSemantics.test.mjs now has 11 guards covering modal wiring, labelling, the self-implemented exceptions, the do-not-dismiss-mid-import cases, every icon-only button and title/label agreement; 11 more in useModalDialog.test.mjs. CLAUDE.md gained the hook to its map and two Common Pitfalls, README lists the three new suites. 10 components, CLAUDE.md, README.md, tests/ui/modalSemantics.test.mjs.
T-0070 | YES | 2.40 | Automation rules, replacing the dead webhooks UI with something that runs: a rule is three plain-language choices (trigger, any number of ANDed conditions, one action) over vocabularies that read as English, evaluated by the pure functions/src/automations.js — which also turns a rule back into one sentence for the editor to preview, refuses to save an incomplete one with a sentence saying what is missing, and catches a rule that would set off its own trigger. The runner (functions/automations.js, onTaskAutomations) loads the workspace rules, performs update / create-follow-up / notify, marks its own writes so a rule can never loop, and logs every run with the sentence and what happened. Rules are admin-only to write and member-readable so nobody is surprised; the run log and notices are read-only from a browser. functions/{index,automations}.js, functions/src/automations.js(+test), firestore.rules, firestore.indexes.json, tests/rules/automations.rules.test.mjs; 28 tests added.
T-0071 | YES | 2.20 | Settings → Automations is now a working panel on top of the T-0070 API: a rule is built entirely from dropdowns (trigger, ANDed conditions, action) with every id shown as the name a person recognises, the rule read back as one English sentence that updates as you build it, and a save button that stays disabled with a plain sentence saying what is missing. Three ready-made starters prefill the editor on an empty workspace, the webhook action offers the connections you already have by name instead of an id, the run log shows what each rule did (subscribed only while open), and "Notices for you" surfaces what the Tell-someone action wrote so that action is not a dead end. The editor adopted useModalDialog, and a half-finished condition now reads "its project is not chosen yet" rather than a pair of empty quotes. Also fixed the Switch-to button in Settings → Workspaces, which threw on every click because setActiveWorkspaceId was never imported. Acceptance steps 2-3 (the follow-up appearing within 10 s) need npm run deploy:functions on the Blaze plan — the runner itself is T-0070 and covered by its 28 tests. src/components/AutomationsSection.jsx, src/components/SettingsView.jsx, src/services/firebase.js, src/App.css, functions/src/automations.js(+test), dev/automations.{html,jsx}, tests/ui/automations.test.mjs; 20 tests added.
T-0072 | YES | 0.45 | Tests and docs for NEW-002: 16 new tests in functions/src/automationFlow.test.mjs walk the acceptance path through the pure chain — taskEventsFor → rulesFor → planAction — proving that completing a task under a "create a follow-up" rule yields exactly one follow-up carrying the parent's workspace, project, phase and priority, that a switched-off rule or an unmet condition yields nothing, that finishing a task twice does not make a second one, and that the two loop guards hold; the last five hold the runner to performing that plan rather than deciding for itself. Docs: README gained an Automations section (the three-choice table, who may write rules, the run log, the notices, the no-runaway guards, and that nothing runs until deploy:functions), three test-table rows and the new dev harness row; CLAUDE.md gained the automations/automationRuns/notifications schemas, an Automations bullet in cross-cutting features, the component in the file map, the Settings view entry and three Common Pitfalls (never a second copy of the vocabulary, never an id on screen, never an action with nowhere to land). Full suite 815 pass; rules suite 74 pass. functions/src/automationFlow.test.mjs, README.md, CLAUDE.md, CHANGELOG.md.
T-0073 | YES | 2.60 | An attachment now belongs to the workspace, not to whoever uploaded it. Uploads go to workspaces/{workspaceId}/{taskId|general|logo}/… — built by the new pure src/services/uploadPaths.js, which refuses a path with no workspace and cannot be talked out of its folder by a hostile filename or task id — and storage.rules decides access by reading that workspace document, so any member can open, replace or delete the file; the old users/{uid}/ prefix is frozen legacy (its uploader can still read and delete, nothing new is written there, existing links keep working). Deleting an activity or a batch of them, and taking an attachment off while editing one, now delete the stored objects too — after the batch commits, so a file the bucket refuses cannot block the record from going. The workspace-logo upload moved into the workspace's own folder and is disabled with a plain sentence until the workspace exists. Verified against the real Storage emulator: 12 emulator cases including the finding itself (the owner deletes a file a member uploaded) and the four ways somebody outside the workspace is refused. Also serialized npm run test:rules — with singleProjectMode the emulator has one bucket, and running the files in parallel let one wipe another's fixtures. src/services/uploadPaths.js(+test), storage.rules, src/services/firebase.js, src/components/{FileUpload,ActivityLogger,ActivityEditor,WorkspaceEditor}.jsx, firebase.json, package.json, tests/rules/{harness,attachments.rules.test}.mjs, tests/ui/attachments.test.mjs, README.md, CLAUDE.md; 45 tests added (16 pure, 17 UI, 12 emulator).
T-0074 | YES | 2.00 | Custom fields now leave the task editor: the new pure src/services/customFields.js describes a project's own fields once — labels (two projects with a Client field are disambiguated by project name), values (a number sorts as a number, a blank sorts last and never becomes an empty chip), chips for a card and columns for a table — and every surface reads from it. Board cards show up to three "Client: Acme" chips under the tags; the Task table's column catalogue now merges the built-ins with one column per custom field, so a field can be ticked on, sorted (numbers numerically, right-aligned), grouped by when it is a select field, and saved in a view — a view whose field was since deleted still opens, just without that column; and every task export (Board, Gantt, Task table) appends the fields as columns to both the readable document and the spreadsheet. tableViews' functions all take the render context now so the catalogue is never a fixed list. src/services/customFields.js(+test), src/services/tableViews.js, src/services/taskExport.js, src/components/{Board,GanttView,TasksTableView}.jsx, src/App.css, tests/ui/{customFields,tasksTable}.test.mjs, README.md, CLAUDE.md; 36 tests added (18 pure, 18 integration). Lint count unchanged at 77 pre-existing problems.
T-0075 | YES | 2.30 | Mentions and the inbox, data layer: the new pure src/services/mentions.js works out who a message is for (an @token resolves against a member's display name, first name or email local part; an email address in the text is not a mention; two people who would share a handle means exactly one is told, never the wrong two; the author never hears about their own message; somebody both mentioned and watching is told once, as a mention) and builds the whole sentence the inbox shows, so an old notice still reads correctly after the wording changes. firebase.js raises those notices where the message is written — addTaskComment takes the audience already on screen, notifyAssignment tells whoever was just put on a task — always after the write and with each notice's failure swallowed, so a notice that cannot be written never costs somebody their comment; markAllNotificationsRead clears the inbox in one batch. firestore.rules now lets a member create a notice, but only inside a workspace they belong to, only for a member of it, only from themselves, only unread, only kind mention/comment/assignment (automation stays the function's) and at most 300 characters. The automation runner's notice took the same shape so one inbox shows both. src/services/mentions.js(+test), src/services/firebase.js, firestore.rules, functions/automations.js, tests/rules/notifications.rules.test.mjs, tests/ui/inboxApi.test.mjs, README.md, CLAUDE.md; 44 tests added (22 pure, 10 data-layer, 12 emulator). UI is T-0076.
T-0076 | YES | 2.50 | The inbox, built on T-0075: a 📥 bell in the topbar with an unread count, and a panel (InboxPanel, split out so a dev harness can render it) listing every notice with its kind named in words — Mentioned you / New comment / Assigned to you / An automation ran — the sentence, and how long ago; opening a row goes to that task and marks it read, Mark all as read clears the rest, Escape and a click outside close it, and an empty inbox says what will land in it. Typing @ in a comment now opens a picker of the people in the workspace (arrow keys, Enter/Tab, Escape, role=listbox) — and it inserts a handle nobody else answers to, because offering @mia where there are two Mias would notify the first one whoever you clicked. Posting a comment hands addTaskComment the audience already on screen, and saving an assignment calls notifyAssignment, so both raise real notices. Navigating to a task moved into services/openTask.js and both the search results and the inbox use it. The duplicate Notices-for-you block was removed from Settings → Automations, which now points at the inbox. Verified end to end in Chrome via the new dev/inbox.html harness: choosing Mia inserted @mia, and posting raised a mention for Mia and a comment for the task's creator, nothing for the author. src/components/{InboxBell,InboxPanel,Markdown,TaskEditor,AppShell,Board,AutomationsSection}.jsx, src/hooks/useInbox.js, src/services/{openTask,mentions}.js, src/App.css, dev/inbox.{html,jsx}, tests/ui/{inbox,automations}.test.mjs, src/services/mentions.test.mjs, README.md, CLAUDE.md; 26 tests added. Lint unchanged at 77 pre-existing problems.
T-0077 | YES | 0.45 | Tests and docs for NEW-004: the new tests/ui/inboxFlow.test.mjs walks the acceptance path in one file — A comments naming B, exactly one mention notice is produced for B with the sentence it will show, A hears nothing about their own message, the task's creator is told separately as a comment, the real InboxPanel renders B's notice as unread, clicking it asks for that task on that task's board, a listener shaped like the Board's opens the right task, and a read notice stops counting and stops offering Mark all as read — plus four guards that the README explains the feature (including that nothing is emailed), lists the three new suites and the harness, that CLAUDE.md carries the notifications schema and the three traps, and that the changelog records both halves. README also gained the Inbox and @mentions section in T-0076; this row added the flow suite to its table. tests/ui/inboxFlow.test.mjs, README.md, CHANGELOG.md; 11 tests added. Full suite 953 pass, rules suite 98 pass, lint unchanged at 77.
T-0078 | YES | 1.70 | Workload planner, data layer: the new pure src/services/workload.js builds the people × next-six-weeks grid — everybody in the workspace gets a row even with nothing on (a free person is exactly who a manager is looking for), an unassigned lane appears only when there is something in it and always sorts last, a task counts in the week its deadline falls in, a task on two people costs both of them, hours already logged come off what is left, a finished/deleted/archived task weighs nothing, a task with no due date is handed back as unscheduled rather than silently dropped, and one beyond the window is neither placed nor called unscheduled. Load is a word not a shade (free / room to spare / a full week / more than a full week), each with a sentence, and describeCell writes the whole thing out for a tooltip and a screen reader. moveTaskPlan turns a drop into a patch: another week keeps the weekday (a Friday deadline stays a Friday) and carries plan.startDate so a three-day task stays three days, another person replaces assignedTo, the unassigned lane clears it, and a drop that changes nothing returns null so no write happens — dotted paths, so plan.startDate is never wiped by a whole-object write. firebase.js gained applyTaskMove, which is the one place a move happens: the write, then the notice for whoever just inherited the task. src/services/workload.js(+test), src/services/firebase.js, tests/ui/workloadApi.test.mjs, README.md, CLAUDE.md; 37 tests added (30 pure, 7 data-layer). UI is T-0079.
T-0079 | YES | 2.40 | New Board → Workload page: people down the side, the next six weeks across the top, each cell showing the tasks planned to end that week, the hours they add up to, and how full that makes the person — coloured with the key printed in words beside it. Week navigation honours the week-start preference; a project filter narrows the whole grid; tasks with no due date are listed underneath with what to do about them rather than being left out; the grid is a real table with scope=col/row and every cell carrying its own sentence as title and aria-label, and it scrolls inside itself on a narrow screen. Dragging is dnd-kit: a chip is both the drag handle and the way into the task (an inner button would have made a chip that small undraggable), a ref stops the click at the end of a drag from also opening the editor, user-select is off so a drag does not select the words it is carrying, the drop target is is-drop-target rather than reusing the over-capacity is-over, and a drop goes through applyTaskMove so the person who just inherited the task is told. Verified in Chrome through the new dev/workload.html harness: dragging a task from an overloaded person to another dropped them from 50h (red) to 30h and wrote {plan.endDate, assignedTo}; dragging a two-day task three weeks out kept the weekday and the length ({plan.endDate: 2026-10-08, plan.startDate: 2026-10-06}). src/components/WorkloadView.jsx, src/services/workload.js, src/App.jsx, src/components/AppShell.jsx, src/App.css, dev/workload.{html,jsx}, tests/ui/workload.test.mjs, README.md, CLAUDE.md; 23 tests added. Lint unchanged at 77.
T-0080 | YES | 0.40 | Tests and docs for NEW-005: the new tests/ui/workloadFlow.test.mjs walks the acceptance path — the grid shows Ace at 50h and marked over while Mia's week is free, the cell dropped on names both the person and the week, the patch is exactly {plan.endDate, assignedTo} (the acceptance line), and the grid rebuilt from the patched task shows Ace down to 20h/room-to-spare and Mia up to 38h/a-full-week, so the load really moved; plus a multi-day task keeping its weekday and its length, and the toast naming the person who now has it. Four docs guards cover the README's new Workload section (including the arithmetic a manager is trusting — four hours a task, forty a week), the three new suites and the harness, CLAUDE.md's module map entry and the two traps the row cost to find, and both changelog entries. tests/ui/workloadFlow.test.mjs, README.md, CHANGELOG.md; 10 tests added. Full suite 1023 pass, rules suite 98 pass, lint unchanged at 77.
T-0081 | YES | 2.60 | Read-only share links, data layer. The design decision that makes this safe: a link holds a SNAPSHOT, never a window — sharedViews/{token} carries the rows it shows, so nothing in tasks, projects or workspaces opens up and a leaked token leaks one project's headline state and never grows into more. The new pure src/services/shareLinks.js (which imports nothing at all, so what leaves the workspace is testable) generates a 26-character token from crypto.getRandomValues, builds the snapshot from a written-down field list — id, title, status, priority, progress, start, end, phase — and nothing else, drops deleted/archived/other-project tasks, counts total/done/overdue so the public page needs no arithmetic, and says in a sentence exactly what the publisher is handing out. firestore.rules gained the app's only world-readable rule, written before the code: get only and never list (a token cannot be found by browsing), live only (revoked or expired is refused in the rules, not merely hidden by a page), create only by a workspace admin in their own name and never born dead, update unable to move a link to another workspace or another publisher. firebase.js gained create/refresh/revoke/setExpiry/delete/subscribe plus getSharedView, which returns null identically for missing, revoked and expired so the page cannot leak which. src/services/shareLinks.js(+test), firestore.rules, src/services/firebase.js, tests/rules/{harness,sharedViews.rules.test}.mjs, tests/ui/shareLinksApi.test.mjs, README.md, CLAUDE.md; 55 tests added (24 pure, 14 data-layer, 17 emulator) — including a guard that this stays the only 'allow ...: if true' in the whole rules file. UI is T-0082.
T-0082 | YES | 2.50 | The sharing UI, on top of T-0081. Project editor → "Share with a client": pick board or timeline, pick how long (7/30/90 days or until you turn it off), and the confirm dialog spells out what you are about to hand out before the link exists — the tasks and their dates, never comments, attachments, hours or who is on what. Publishing copies the URL; every link then shows live/off, how long it has left and its own URL, with Copy, Refresh (a stale snapshot is fixable without issuing a new URL) and a danger-marked Turn off that says it takes effect straight away. An editor sees who can publish instead of a button that would fail. The public page is a new route rendered BEFORE the sign-in gate — asking a client to sign in is the one thing it must not do — split into SharedViewPage (fetches exactly one document) and SharedSnapshot (renders it, fetches nothing), the latter drawing either a three-column board or a timeline whose bars are laid out by the pure buildBars, with overdue in red, done in green and "No dates yet" rather than a bar that pretends. Missing, revoked and expired all say the same sentence, so a stranger cannot learn that a token was once real. Verified in Chrome: both forms render from a sample snapshot via the new dev/shared.html harness, and #/shared/<token> on the real app renders signed-out with no sign-in screen. NOTE: the sharedViews rule must be deployed (npm run deploy:rules) before the first real link will open. src/components/{SharedViewPage,SharedSnapshot,ShareLinksPanel,ProjectsView}.jsx, src/App.jsx, src/services/shareLinks.js, src/App.css, dev/shared.{html,jsx}, tests/ui/shareLinks.test.mjs, README.md, CLAUDE.md; 24 tests added. Lint unchanged at 77.
T-0083 | YES | 0.45 | Tests and docs for NEW-011: the new tests/ui/shareFlow.test.mjs walks the acceptance path with tasks deliberately carrying every private field the app has — assignees, an external contractor, requestedBy, logged hours, counters, tags, custom values, an internal description and an attachment link — and proves the published document contains none of them, the rendered page shows none of them, a deleted task and another project's task are not in it, the timeline renders both bars with the project name and counts, the page has no button, no input and no link anywhere in it (nothing to press, nothing to type, no way back into the app, no sign-in), it says it is a snapshot and when it was taken, the board form renders the same snapshot as three columns, and a revoked or expired link fails the same test the server applies. One test names the deliberate exception: the publisher's own name and id are the only identities that travel — the client is told who shared it and the rules need the id to stop a link being reassigned — and neither appears inside the snapshot itself. Four docs guards cover the README's Sharing section (including what it never shows, the deploy:rules prerequisite and the one-identity rule I added while writing this row), the new suites and harness, CLAUDE.md's schema and three pitfalls, and both changelog entries. tests/ui/shareFlow.test.mjs, README.md, CHANGELOG.md; 15 tests added. Full suite 1100 pass, rules suite 115 pass, lint unchanged at 77.
T-0084 | YES | 2.30 | Recurring tasks now come round on schedule, not only when the last one was ticked off — a weekly ritual nobody completed used to die at the first missed week. The new pure src/services/recurrenceSchedule.js groups tasks into series (keyed on recurrenceParentId, anchored on the instance scheduled furthest ahead), walks forward from where each series actually got to, and returns every occurrence that has become due and is missing, as payloads ready for addTask. Bounded on purpose: an occurrence appears on the day it is due and never early (HORIZON_DAYS 0 — a week-long horizon would put next week's copy beside this week's), nothing older than 30 days is created so a dormant series does not produce a year of overdue copies, at most 8 per series per run, and the walk that crosses the gap is capped so a decade-dormant daily series returns rather than looping. firebase.js gained materialiseRecurrences, which writes through addTask (never a hand-rolled document) and survives one series failing; useRecurrenceCatchUp runs it on load and hourly, once per day per device, retrying after a failure, reading the app-wide tasks subscription so it costs no extra query, and logging rather than interrupting. The on-completion path is untouched — this is a second way, and both key on recurrenceParentId + due date so they are safe together. DECISION FOR REVIEW: the audit's fix called for a Cloud Scheduler function; I ran it in the app instead (the audit's own stated fallback) to keep this a static front end with one narrow functions deployment. The honest limit, stated in the code and the README: a workspace nobody opens for a month catches up the next time somebody opens it, not before. Verified in Chrome through the new dev/recurrence.html harness: on the 20th the weekly one is correctly not due; on the 21st it appears with the 14th's instance still untouched at 'todo'; a week later the daily series catches up all seven missed days plus the weekly one, eight in total, inside the per-run cap. src/services/recurrenceSchedule.js(+test), src/services/firebase.js, src/hooks/useRecurrenceCatchUp.js, src/App.jsx, dev/recurrence.{html,jsx}, tests/ui/recurrenceCatchUp.test.mjs, README.md, CLAUDE.md; 40 tests added (22 pure, 18 integration). Full suite 1140 pass, rules 115 pass, lint unchanged at 77.
T-0085 | YES | 0.55 | useModalDialog gained an `open` flag (default true) whose effect is a complete no-op when false — no document-capture key listener, no focus moved in, no focus restored — plus a belt-and-braces guard so the Escape branch returns early when ref.current is null, meaning a call site that forgets the flag still cannot stopPropagation() a key it has no panel to close. That capture-phase stopPropagation at document was killing Escape for the whole app. src/hooks/useModalDialog.js, tests/ui/useModalDialog.test.mjs; 5 tests added (1152 pass).
T-0086 | YES | 0.95 | TimerWidget — the one component in the app that called useModalDialog above its own early return, and the one AppShell mounts for the whole life of the session — now passes open: confirmOpen, so its document-capture Escape handler exists only while its stop dialog is on screen. Audited the other 24 call sites: all are dedicated modal components their parent renders conditionally, so none needs the flag, and a new guard in tests/ui/escapeKey.test.mjs fails the build if any component ever calls the hook above a top-level `return null` without passing open. Verified in Chrome through the new dev/escape-key.html harness, which mounts the real TimerWidget beside stand-ins for the five real Escape consumers registered exactly where the real ones are: all five counters rose on each press with no dialog open AND with the timer running, then held still for the one press that closed the stop dialog, focus returning to the Stop button. src/components/TimerWidget.jsx, dev/escape-key.{html,jsx}, tests/ui/escapeKey.test.mjs; 7 tests added (1159 pass).
T-0087 | YES | 0.30 | Tests and docs for BUG-012. Twelve tests now cover it across two files: five in useModalDialog.test.mjs hold the hook itself (a closed modal lets Escape through, a call site that forgets the flag still cannot swallow it, an open one closes and stops there, a closed one moves nobody focus, and opening then closing installs and removes the trap), seven in the new tests/ui/escapeKey.test.mjs walk the acceptance path with the real TimerWidget plus the five real Escape consumers registered where the real ones are, and three guard the docs. Confirmed the tests catch the original bug by reverting both halves of the fix — 3 of them fail, and pass again once restored. README gained the suite and the harness row; CLAUDE.md gained the pitfall (with the document-capture reason that makes it so far-reaching) and the hook-map note. NOTE: the row named server/src/app.test.ts and Implementation_plan.md, neither of which exists in this repo — this is a static SPA with no server, and the plan document is FEATURE_ROADMAP.md. tests/ui/escapeKey.test.mjs, README.md, CLAUDE.md, CHANGELOG.md; 3 tests added (1162 pass).
T-0088 | YES | 1.10 | Finishing a recurring task now means the same thing whichever button you press. The gate lives in the pure services/recurrence.js as shouldSpawnRecurrence(before, after) — recurring, just became done, not already done, not being deleted or archived — alongside alreadySpawned(payload, siblings), which is the same recurrenceParentId + plan-dates key the scheduled catch-up pass uses, so whichever path gets there first the other finds the occurrence and does nothing. firebase.js exports maybeSpawnRecurrence(before, after) over the two, and both setTaskStatus (Board drag-and-drop, activities modal) and TaskEditor Save call it; spawnNextRecurrence dedups through the shared rule instead of its own inline copy. The editor passes the task as it is AFTER the save, so an occurrence created there uses the dates and the rule just typed rather than the ones the modal was opened with — verified by a test where moving the due date to a Wednesday in the same save that finishes it yields the 28th, not the 21st. A failed spawn is caught and never costs the save. src/services/recurrence.js, src/services/firebase.js, src/components/TaskEditor.jsx, tests/ui/recurrenceOnSave.test.mjs, tests/ui/recurrenceCatchUp.test.mjs, README.md; 15 tests added (1177 pass).
T-0089 | YES | 0.85 | New pure src/services/ganttGeometry.js: the rule is that a plan with one date is a one-day MILESTONE on that date, not nothing. effectivePlan() resolves the span a task actually has (due-only, start-only, a real range, both-same-day, and a backwards range that used to draw a negative bar — the due date wins), planBar() turns it into left/width in pixels and is never narrower than one day column, dragOrigin/dragTo/dragPatch carry the drag: a milestone now HAS an origin, so dragging its left edge produces {plan.startDate, plan.endDate} where there was no start date before — the acceptance criterion — while the left edge can never pass the due date nor the right edge the start date, a drag that changes nothing writes nothing, and the patch uses dotted paths so the actual dates survive. planLabel() is honest about a date the user never set rather than claiming a range they never typed. Date arithmetic is local-day throughout (a Manila morning is not the day before) and rubbish input returns null instead of an Invalid Date. src/services/ganttGeometry.js(+test); 22 tests added (1199 pass). UI is T-0090.
T-0090 | YES | 1.40 | GanttRow now draws from the pure ganttGeometry module instead of its own inline arithmetic, so a task with only a due date gets a one-day marker on that date rather than a row with a title and a blank track — the commonest task in the app, because quick-add and the natural-language parser only ever write an end date. The marker is class "milestone" (brighter, outlined, min-width 12px so it stays visible and grabbable at month zoom where a day column is six pixels) and deliberately NOT rotated into a diamond: the left and right resize handles have to stay where a user reaches for them, since dragging the left edge is how a due-only task gets a start date. startDrag no longer refuses a task without both plan dates — dragOrigin gives a milestone a real origin — and the commit goes through dragPatch, which writes dotted paths and returns null when nothing moved. The tooltip and a new aria-label say "Due 2026-09-25 · no start date yet — drag the left edge to set one" rather than inventing a range. GanttRow is exported with an onSavePlan seam (defaulting to the real updateTask) so one row can be driven with no Firestore. Verified in Chrome via the new dev/gantt.html: three rows that used to be blank now show markers, the no-plan row correctly shows none, and a real PointerEvent drag of row 1 left edge wrote plan.startDate 2026-09-22 / plan.endDate 2026-09-25 and the row became a four-day bar. src/components/GanttView.jsx, src/App.css, dev/gantt.{html,jsx}, tests/ui/ganttMilestone.test.mjs, README.md; 16 tests added (1215 pass).
T-0091 | YES | 0.25 | Tests and docs for BUG-014. Thirty-eight tests now cover it: 22 pure ones in src/services/ganttGeometry.test.mjs (every shape of plan including a backwards range, the one-column width, the drag clamps at both edges, a drag that writes nothing, a milestone dropped back where it started that DOES write because it gains a start date, dotted paths, local-day arithmetic across month and year boundaries, and rubbish input returning null rather than an Invalid Date) and 16 in tests/ui/ganttMilestone.test.mjs that render the real GanttRow and drive real pointer drags over it, including the acceptance write and the bar following the pointer mid-drag. Three more guard the docs. CLAUDE.md gained the module in the file map and a pitfall stating the rule and why the old requirement was wrong; README lists both suites and the harness. tests/ui/ganttMilestone.test.mjs, CLAUDE.md, README.md, CHANGELOG.md; 3 tests added (1218 pass).
T-0092 | YES | 1.20 | The Status column the wizard offers, guesses from a heading and shows in the preview is now the status the task gets. Three changes, all root-cause: (1) a new pure src/services/taskStatus.js holds statusStamps(status, { today, current }) — the one rule for what a status implies about progress and the actual dates, with "something finished must have started" so an imported Done row is not left open-ended, and no clock of its own so a test can pin the day; addTask and setTaskStatus both derive from it, replacing setTaskStatus inline copy. (2) addTask takes an optional status, normalized against todo/doing/done and defaulting to todo, so quick-add, templates, the AI create path and the recurrence spawn — none of which passes one — behave exactly as before. (3) the import payload moved into importedTaskPayload() in services/csv.js, beside IMPORT_KINDS, so the list of fields the wizard offers and the list it writes cannot drift apart; a guard test fails the build if a field is added to IMPORT_KINDS.tasks without a home in the payload, which is precisely how Status came to be mapped, previewed and thrown away. Verified the whole path in one test: a CSV Status heading is recognised without being told, "Done" and "In progress" parse to done and doing, a blank cell falls back rather than failing the row, the summary counts all three as willImport, and a Done row yields progress 100 with both actual dates. src/services/taskStatus.js(+test), src/services/csv.js, src/services/firebase.js, src/components/ImportWizard.jsx, tests/ui/importStatus.test.mjs, README.md, CLAUDE.md; 22 tests added (1240 pass).
T-0093 | YES | 0.70 | ⌘K → "Log an activity" now opens the task picker instead of navigating to Work Performed and stopping. WorkPerformedView listens with useQuickCreate("activity", …). Root cause was three separate lists — the palette vocabulary, the destination views (a private VIEW_FOR table inside AppShell) and the pages listeners — so nothing failed when an entity had no receiver; CREATE_ORDER and a new CREATE_VIEW now live together in services/commandPalette.js, AppShell imports the destinations rather than keeping its own copy, and a repo-wide guard fails the build if any entity in CREATE_ORDER has no useQuickCreate listener anywhere in src/components or no destination. Four DOM tests render the real page and fire the real event: it opens the picker, a request for a task leaves the page alone, and asking again after Escape still works. Also fixed a realm bug in the shared test harness while doing it — app code builds CustomEvents (quick-create, task:done, open-task) and jsdom refuses one built from Node own constructor, so dom.mjs now hands the window CustomEvent, KeyboardEvent and PointerEvent to globalThis, and the one test that dispatched on a bare Node EventTarget uses the window own. src/components/WorkPerformedView.jsx, src/services/commandPalette.js, src/components/AppShell.jsx, tests/ui/{quickCreateActivity,commandPalette,inboxFlow}.test.mjs, tests/ui/dom.mjs, README.md; 8 tests added (1248 pass).
T-0094 | YES | 1.05 | The AI errors themselves stop carrying developer copy. In services/ai.js the bridge-unreachable error and the HTTP error each now throw a plain sentence as their message and put the operator version — the bridge address, the cause, `npm run bridge`, the raw upstream body — on err.detail, for console.error and a bug report. The new describeAiFailure(err, fallback, { isOperator }) in the pure services/errorMessages.js turns any AI failure into { message, detail, code, isOperatorMessage } with a clear order of preference: an operator is shown the thing they can act on (the same rule as knowledgeCopy({ isOperator }), because hiding the command from the person who can fix it helps nobody); otherwise a sentence we wrote ourselves that is already safe wins — noKeyMessage names the company and who to ask, which beats anything generic — then the sentence for that kind of failure (401/403 denied, 402 not set up, 408/504 timeout, 429 busy, 5xx trouble, other 4xx refused, abort, offline), then the caller fallback. Two guards make it hard to undo: looksWritten() refuses to promote a thrown word like "aborted" into copy, and isPlainUserMessage() rejects a backtick, npm/npx/yarn, a URL or localhost or a port, a bare HTTP status, the words anthropic/claude code/CLI/bridge, and an SDK dump — every sentence the module can produce is tested against it, including the exact old message, which is now caught even if something threw it again. src/services/ai.js, src/services/errorMessages.js(+test); 21 tests added (1264 pass). UI is T-0095.
T-0095 | YES | 1.60 | Every AI surface now shows one plain sentence and keeps the operator version in the console. The audit named two call sites; the new guard in tests/ui/copy.test.mjs found seven more of the same bug, and a guard with six exemptions is not a guard, so all nine are fixed: Dashboard "Plan my day", the topbar AI bulb (twice), the AI task generator, the task AI panel (twice), Review → AI assist, the due-task alert prompt and its notebook save, and Settings → Knowledge base (twice — operator-only, so it passes isOperator: true and still shows the CLI own words, but the decision is made in one place rather than by rendering a thrown message by hand). Two new guards in copy.test.mjs fail the build on setError(err.message) and on {error.message} rendered into JSX, which is what the toast-only guard always missed. Also found and fixed the sign-in page while there: signInWithGoogle returned the Firebase SDK message verbatim to a signed-out stranger, and told them to "Add it in Firebase → Auth → Settings → Authorized domains" — both now go through friendlyError / the console, and LandingView state is renamed `problem` because it never was a caught error. A new hooks/useUserProfile export, isOperatorProfile() / useIsOperator(), replaces the hand-written role check SettingsView kept (a third copy was about to appear), guarded by a test. Refined the precedence in describeAiFailure while writing the rendered test: knowing what went wrong beats what the thrower happened to say — "The AI service refused that request." is true of a 429 but "wait a few seconds" is what the person needs — with PREFER_OWN_MESSAGE as the named exception for no-api-key, where our own sentence names the company. 9 components, src/services/firebase.js, src/services/errorMessages.js, src/hooks/useUserProfile.js, tests/ui/{aiErrorCopy,copy}.test.mjs, README.md; 17 tests added (1281 pass).
T-0096 | YES | 0.30 | Tests and docs for BUG-020. Thirty-eight tests now cover it: 21 pure ones in src/services/errorMessages.test.mjs (every status, timeout, offline, a thrown word that is not copy, a sentence we wrote ourselves that is, the operator path, the precedence rule and its named exception, and the exact old bridge message which is now caught even if something threw it again), 13 in tests/ui/aiErrorCopy.test.mjs that render the real error card and assert no backtick, no npm, no URL, no port, no provider name and one sentence rather than a paragraph — plus a per-surface check over all seven AI components that each describes its failure and still logs the operator detail, and one holding the sign-in page to the same rule. Four guards in copy.test.mjs make the pattern unrepeatable. CLAUDE.md gained errorMessages.js and useUserProfile.js to the "Telling the user something" map and two pitfalls; README lists the new suite. NOTE: the visibly-degraded MOCK reply still prints shell commands in its body on an AI surface — that is content, not an error message, and it is already queued as T-0112, so it is left for that row rather than widened into this one. tests/ui/aiErrorCopy.test.mjs, CLAUDE.md, README.md, CHANGELOG.md; 3 tests added (1284 pass).
T-0098 | YES | 0.75 | useModalDialog stops stealing the caret. Its effect moves focus into the dialog, so it has to run once per open — but it was keyed on onClose, and almost every call site passes a fresh arrow, so every render of the parent re-ran it. Board re-renders whenever the workspace tasks listener fires (a teammate edit, a counter bump, the user own timer), and each one yanked the caret back to the modal first field mid-edit. onClose, closeOnEscape and autoFocus now live in a ref the key handler reads at the moment it fires, and the dependency list is [open] and nothing else — the same pattern DueTaskAlertModal already used deliberately. Tests written before the change, per the Medium risk: six new ones cover the caret staying put over three successive re-renders, a half-made text selection surviving one (moving focus would have collapsed it), Escape and the backdrop click both reaching the NEWEST onClose rather than the one captured on the first render (a ref that is never updated is the other half of this bug), closeOnEscape being flipped mid-modal without moving focus — the import wizard does that while writing — and focus still being moved in once on open, so the fix costs no keyboard user their way in. Confirmed the bug first: an isolated run showed focus jumping from "last" to "first" on a re-render, and the full file ran to a SIGKILL because the focus ping-pong never settled; both are gone. src/hooks/useModalDialog.js, tests/ui/useModalDialog.test.mjs, CLAUDE.md; 6 tests added (1290 pass, all 23 modals unchanged).
T-0099 | YES | 0.60 | The shared half of BUG-017: the seed mechanism now lives in one place instead of being written inline in TaskForm and nowhere else. hooks/useQuickCreate.js gained newSeed(text) — the { text, at } shape, where the timestamp is what makes asking twice with the same words refill the field — and useSeededField(seed, apply), which applies a seed exactly once. Two things the inline version got wrong and this does not: it initialises its "seen" marker to null rather than to the seed, so a form that MOUNTS with a seed already set (which is what happens when the command itself opens the editor) applies it on that first render instead of waiting for a second one that never comes; and it is a render-phase update, so the field is filled before the browser paints rather than flashing empty first. Board and TaskForm were moved onto it, and a repo-wide guard fails the build if anyone writes { text, at: Date.now() } by hand again. src/hooks/useQuickCreate.js, src/components/{Board,TaskForm}.jsx, tests/ui/quickCreateSeed.test.mjs, tests/ui/commandPalette.test.mjs, README.md; 10 tests added (1300 pass). The three pages that throw the text away are T-0100.
T-0100 | YES | 0.95 | ProjectsView, MinutesView and GoalsView stopped throwing away what the user typed. Each listener now takes the text, stores it as a seed beside the existing `editing` state and hands it to its editor, which applies it with useSeededField from T-0099: the project name, the minutes title and the goal title respectively. Three details that matter: an empty ask seeds null rather than an empty string, so a bare "new project" opens a blank form instead of writing "" over a default; the seed is passed only when editing === "new", so opening an EXISTING project can never have its name overwritten; and closing clears the seed, or reopening the editor would refill it. The goal editor needed care — its first field is a short code, not the title — so the render tests target each field by the placeholder the user actually sees rather than by "the first input". Six render tests drive the real pages through the real event and read the real field, plus five structural guards per page and the repo-wide guard that every quick-create listener takes the text at all (activity excepted: it opens a picker, not a form). src/components/{ProjectsView,MinutesView,GoalsView}.jsx, tests/ui/quickCreateSeed.test.mjs; 10 tests added (1310 pass).
T-0101 | YES | 0.20 | Tests and docs for BUG-017. Twenty-three tests cover it in tests/ui/quickCreateSeed.test.mjs: the seed shape and the empty-ask case, applying it once (including a re-render with the SAME seed not undoing what the user has typed since, and asking twice with the same words still refilling), the trip through the real event, six render tests that drive the three real pages through the real event and read the real field, five structural guards per page, the repo-wide guard that nobody builds a seed by hand, and the one that every quick-create listener takes the text. CLAUDE.md gained the pitfall; README lists the suite. tests/ui/quickCreateSeed.test.mjs, CLAUDE.md, CHANGELOG.md; 3 tests added (1313 pass).
T-0102 | YES | 0.65 | New pure src/services/tagFilter.js: the rule for "show me only #client", written once so every page can mean the same thing. The interesting part is the Activity Log — an activity carries no tags of its own, it borrows them from the task it was logged against, so filtering the log by #client means "entries against tasks tagged #client"; tagsOf(item, { taskById }) handles both shapes, and an activity whose task is gone or has scrolled past the loaded page has no tags and is filtered OUT rather than shown, because showing a #client entry to somebody filtering for something else is the worse mistake. filterByTag is exact (#client never matches #clients) and a tag nobody carries shows nothing rather than falling back to everything — that fallback is how the bug went unnoticed. tagFilterState() hands a page its chip list, the active tag, the filtered items, and a `missing` flag for a saved view pointing at a tag since renamed, so the page can still show the chip and let it be cleared instead of showing an empty screen with no explanation. Board was moved onto it, replacing its two inline copies. src/services/tagFilter.js(+test), src/components/Board.jsx, README.md; 14 tests added (1327 pass). Gantt and the Activity Log are T-0103.
T-0103 | YES | 1.55 | Gantt, the Activity Log AND the Calendar now honour a saved view tag filter. The audit named two pages and said Calendar already accepted the prop — it did not, and the new guard over App.jsx caught it: the guard reads every component the router passes initialTagFilter to and fails if that component does not declare it, which is the only thing that fails when a prop is simply unused. All four pages now take the prop, hold it in local state re-synced by an effect so opening a second saved view re-applies rather than keeping the first, and render the same new shared TagFilterBar — Board hand-rolled strip was replaced by it, so there is one strip and not four. The Activity Log filters through taskById, because an activity has no tags of its own. Two empty states were written rather than left blank: the Gantt says "No scheduled tasks carry #client" with a clear link, and the Activity Log adds the one thing a user would never guess — that an activity takes its tags from the task it was logged against. A saved view naming a tag nothing on the page carries still shows that chip plus "Nothing here carries #renamed-since", so an empty page always has a visible, clearable reason. Verified in Chrome via the new dev/tag-filter.html: #client showed 2 of 4 tasks and 1 of 4 log entries (the entry against an unloaded task correctly excluded), and ?tag=renamed-since showed the chip, the explanation and the clear link. src/components/{TagFilterBar,Board,GanttView,TableView,CalendarView}.jsx, dev/tag-filter.{html,jsx}, tests/ui/tagFilterViews.test.mjs, README.md; 12 tests added (1339 pass).
T-0104 | YES | 0.20 | Tests and docs for BUG-018. Twenty-nine tests cover it: 14 pure ones in src/services/tagFilter.test.mjs (where the tags come from for a task and for an activity, an activity whose task is gone, exactness so #client never matches #clients, a tag nobody carries showing nothing rather than everything, the sorted de-duped chip list, and the missing-tag state) and 12 in tests/ui/tagFilterViews.test.mjs that render the real strip — the selected chip, both ways to clear it, aria-pressed, no strip at all when there is nothing to offer — plus the structural guards over all four pages and the App.jsx prop-contract guard that found the Calendar. Three more guard the docs. CLAUDE.md gained the module in the file map and a pitfall carrying both halves of the lesson (a prop nobody declares fails nothing; an activity has no tags of its own); README lists both suites and the harness. tests/ui/tagFilterViews.test.mjs, CLAUDE.md, README.md, CHANGELOG.md; 3 tests added (1342 pass).
T-0105 | YES | 0.45 | Settings → Notifications → Send test notification no longer reports success as an error. One helper was delivering every outcome — success included — through toast.error with the internal trail stapled on, so the happy path was a red role="alert" reading "Test notification dispatched" followed by "SW ready: scope=/". Two audiences, two channels now: the log goes to console.debug("[notifications] test trail:"), and the user gets one plain sentence — toast.success("Test notification sent. If you did not see it, check your notification settings for this browser.") when it went, toast.error only when something actually failed. The three failure messages were rewritten as sentences a person can act on rather than API names ("This browser cannot show notifications", "Notifications are blocked for this site. Click the lock icon…"). The macOS/Focus advice moved out of the disappearing toast and under the permission badge, where it can be read at leisure and only when permission is actually granted. Three new guards in copy.test.mjs: no toast may carry a diagnostic trail, no component may assemble a toast out of an internals log (the exact toast.error(msg + log.join) shape), and every message in the handler must be one sentence — the strings were assembled at runtime, which is why the existing guard never caught this. src/components/SettingsView.jsx, tests/ui/copy.test.mjs; 3 tests added (1345 pass).
T-0106 | YES | 0.40 | Settings → Account stopped sending people down a path that does not exist. The invite-by-email work moved the only control that consumes a raw account id behind a superadmin toggle but left this producing control telling every user to "Share this ID with a workspace owner so they can add you as a member" — an ordinary owner has no field to paste it into. The box is now superadmin-only and inside a "Technical details" disclosure with no instruction attached (kept for debugging with support, and given an aria-label since it is a bare input), and everyone sees the sentence that is actually true: ask an owner to invite <their own email address, shown>, the invitation is waiting at next sign-in, nothing to copy or send. Three guards added: no screen may tell a user to share an account id / your id / a uid, the Account section must point at the email invite, and the id must stay behind both the superadmin gate and the disclosure. src/components/SettingsView.jsx, tests/ui/copy.test.mjs; 3 tests added (1348 pass).
T-0107 | YES | 0.50 | The topbar due-alert switch now means what it says. It is presented as the on/off control for due-task alerts, but useOverdueScan read leadDays, snoozes and skips from the same settings object and never checked `enabled`, so a user who turned it off kept getting desktop banners every five minutes for the same tasks. The scan is gated on prefs.enabled (and `enabled` added to the effect deps, so flipping the switch restarts it rather than waiting for a reload), the switch label now reads "off, in the app and on your desktop" rather than claiming less than it controls, and Settings → Notifications says the banners follow that switch — otherwise there would be no discoverable way to turn them back on. Took the audit "simplest" option deliberately: the alternative (a second toggle) leaves the original complaint intact, since the switch a user reaches for would still not stop the banners. Consequence worth noting: `enabled` defaults to false, so desktop banners are now opt-in too — which is correct, because nobody opted into them either. CLAUDE.md, which documented the old split as deliberate, was corrected. src/hooks/useNotifications.js, src/components/DueAlertBell.jsx, src/components/SettingsView.jsx, CLAUDE.md, tests/ui/dueAlertToggle.test.mjs; 6 tests added (1354 pass).
T-0108 | YES | 0.30 | The Board card footer had "+ Log" and "Log" side by side, doing different things — the second toggles the inline activity history and inherited the word from the feature name rather than from what it does, flipping to "Hide" only after being pressed. Neither had an aria-label, so a screen-reader user heard "plus Log" and "Log" with nothing to tell them apart. The toggle is now "History · 4" (the count comes free from the denormalized task.activityCount) / "Hide history", carries aria-expanded so a disclosure announces its state, and both buttons name the task in their aria-label and explain themselves in a title. Exactly one control on the card is called Log, guarded by a test that reads the footer and asserts the list of Log-ish labels is exactly ["+ Log"]. src/components/Board.jsx, tests/ui/copy.test.mjs; 2 tests added (1356 pass).
T-0109 | YES | 0.55 | An unrecognised hash no longer claims to be another page. The topbar fell back to VIEWS[0] — whose position in a cosmetically-ordered list happens to be Ask AI — so #/nonsense, an invite rendering inside the shell, or a saved view pointing at a removed page all painted "Ask AI" over a blank content area. AppShell now exports RENDERABLE_VIEWS (the sidebar list plus the views that render in the shell without a sidebar entry, currently `invite`) and isKnownView(); the topbar falls back to the app own name, which claims nothing. A new NotFoundView says "That page does not exist", names the page that was asked for, explains that the link may be out of date or a saved view may point somewhere removed, and offers one button back to the Dashboard — a blank screen reads as a crash. Two guards keep the two lists honest in both directions: every view App.jsx renders must be a known view (or the not-found card would cover a real page), and every known view must actually be rendered somewhere (or a sidebar entry is a link to a blank page). src/components/AppShell.jsx, src/components/NotFoundView.jsx, src/App.jsx, tests/ui/unknownView.test.mjs; 8 tests added (1364 pass).
T-0110 | YES | 0.60 | addTask stops overwriting what it is handed. T-0092 had already given it an optional status; this row finishes the job for progress and the actual dates, which it still hardcoded to 0 and null. Anything the caller states now wins and statusStamps fills only the gaps, so a template capturing work in progress, an AI-generated backlog with a mix of states, or an imported row marked "not started" that nonetheless records when it began all survive the trip. The subtle part is telling silence from a statement: clampProgress() returns null for undefined, null, an empty cell or "n/a" — but 0 for a real zero — so `statedProgress === null ? stamps.progress : statedProgress` keeps an explicit 0 from being read as "say nothing" and replaced by the status default. A percentage outside 0-100 is clamped rather than rejected: a spreadsheet row saying 150% means finished, and refusing the row would lose the task. An unknown status is still coerced to todo rather than stored. Every existing caller is unchanged — none of the nine passes a status, and the recurrence spawn payload carries none either. src/services/taskStatus.js(+test), src/services/firebase.js, tests/ui/importStatus.test.mjs, CLAUDE.md; 8 tests added (1372 pass).
T-0111 | YES | 0.65 | Settings stopped polling. A setInterval re-read Notification.permission every 1.5 seconds and re-rendered the largest component in the app — workspaces, users, companies, webhooks, automations — for as long as the page was open, with no user input. The Permissions API subscription that useOverdueScan already had was extracted into a shared useNotificationPermission() hook and both call sites use it, so the two can never disagree. Two signals, both free when nothing changes: the change event (the user granting or blocking from the browser own UI, in another tab or in site settings) and a window focus re-read for Safari and anything else that does not list notifications in the Permissions API — a permission is changed in browser chrome, so coming back to the page is exactly when it matters. It reads Notification.permission rather than status.state so one source of truth answers, returns "unsupported" without crashing where there is no Notification API at all, and unsubscribes on unmount (a listener per mount is a leak on a page people revisit). A test mounts the hook, waits well past the old interval and asserts the render count is unchanged, then fires a change event and a focus event and asserts the badge follows. Two guards: Settings must not poll, and no component may put "permission" inside a setInterval. src/hooks/useNotifications.js, src/components/SettingsView.jsx, tests/ui/notificationPermission.test.mjs, README.md; 10 tests added (1382 pass).
T-0112 | YES | 1.10 | The AI layer stopped writing one message for two audiences. T-0094/T-0095 had already split the thrown errors; this row finishes the job for everything that comes back as a successful-but-degraded answer. mockReply body no longer prints "npm i -g @anthropic-ai/claude-code && claude" and "start the bridge with `npm run bridge`" into the answer itself — it now says to ask an administrator — and the runbook moved to an exported MOCK_OPERATOR_HINT that travels on the result as a new `operatorHint` field beside `reason`, rendered by the new <AiOperatorHint> only where useIsOperator() is true. The two degraded sentences everybody sees were rewritten too: GROUND_UNAVAILABLE no longer names the Claude Code bridge, and WEB_UNAVAILABLE renders providerLabel(provider) in words instead of a raw id like "bridge-api". The cached due-alert entry carries the hint, or it would be lost on the second view. The guard is the useful part: no component outside an operator gate may write an npm command, and a second test asserts the two exemptions (AiBrainSection behind {isSuperadmin && …}, KnowledgeSection behind its isOperator early return) still have those gates — an exemption is only valid while the gate exists. Verified in Chrome via dev/due-alert.html after clearing the cached prompt: the placeholder answer now contains no command, package, CLI name or port, and no operator hint, because that session is not a superadmin. src/services/ai.js, src/components/{AiOperatorHint,DueTaskAlertModal}.jsx, tests/ui/aiOperatorCopy.test.mjs, CLAUDE.md, README.md; 9 tests added (1391 pass).
T-0113 | YES | 0.90 | One list of pages, instead of four. The new src/services/views.js holds VIEW_REGISTRY and the sidebar (AppShell VIEWS), the topbar title, the not-found check (RENDERABLE_VIEWS / isKnownView, moved here from T-0109) and the ⌘K palette (NAV_TARGETS, now derived rather than hand-maintained) all read it. NAV_TARGETS had drifted to eighteen entries against twenty-one sidebar views, so Workload, Trash and Artifacts returned nothing in the palette and the only way to them was a collapsed sidebar group — Trash worst of all, since that is exactly where somebody goes the moment they delete something by accident. Every entry now carries the words people actually reach for: trash also answers to deleted / restore / recover / undelete / bin, workload to capacity / who is busy / rebalance / people, artifacts to files / attachments / outputs / documents. A label own words are folded in automatically and de-duped, so "gantt chart" finds Gantt without that phrase being written twice. Three guards: every registry entry must be reachable from the palette (a new page cannot ship unsearchable), the palette must not declare its own list, and every sidebar group child must name a real view. Four existing tests that matched AppShell source text for their VIEWS entry were pointed at the registry data instead, which cannot drift the same way. src/services/views.js, src/services/commandPalette.js, src/components/AppShell.jsx, src/App.jsx, 5 test files, CLAUDE.md; 6 tests added (1397 pass).
T-0114 | YES | 0.70 | One import control on the Activity Log instead of two. "Import CSV" (the fixed-column CsvImporter) sat beside "Import from spreadsheet" (the four-step wizard) with nothing to say which to pick — the kind of choice that makes a non-technical person stop. The wizard supersedes the other for every case, and I proved it before removing anything: the exact ten-column header row exportCsv writes auto-maps to all ten wizard fields with nothing to do by hand, so the fast path the old screen existed for is now the wizard default behaviour. The button is simply "Import"; CsvImporter.jsx and its test file are retired, and its five promises were migrated as tests against the wizard — CSV-only file picker, a wrong-columns file explained without jargon or "undefined" or "-1", the preview naming what will happen with the skipped row and its reason, and nothing written until a button that says "Import 2". Two new guards: the Activity Log header must contain exactly one Import-ish label, and nothing may import the retired component; plus one that keeps exportCsv header row and the import aliases in step, since renaming an exported column without adding the alias would quietly break round-tripping. src/components/TableView.jsx, src/components/CsvImporter.jsx (deleted), tests/ui/CsvImporter.test.mjs (deleted), tests/ui/importWizard.test.mjs, tests/ui/modalSemantics.test.mjs, src/services/csv.js; 7 tests added, 5 retired (1399 pass).
T-0115 | YES | 0.95 | New pure src/services/activityExport.js: buildActivityLogDocument() and buildWbsDocument(), in the same document-model style as taskExport.js, so the four surfaces still hand-rolling a CSV can produce all seven formats. The activity log groups by day newest-first with each day hours in the readable formats and stays flat in the spreadsheet where people sort for themselves; hours are numbers so a spreadsheet can total them (a blank or "n/a" is 0, not NaN); an entry logged against a since-deleted task keeps the title it was logged with, which is the whole reason that snapshot is stored; and an empty log exports a file saying so with its header row intact rather than a blank one. The WBS keeps the project OWN phase order rather than sorting alphabetically — that order is the plan — names an empty phase instead of printing an empty table, gives a task whose phase was deleted a "No phase" home rather than dropping it, and leads with the four numbers a manager asks for. taskPercent/groupPercent reproduce the arithmetic on screen exactly (done is 100 whatever progress says, out-of-range is clamped, an empty group is 0 not NaN). The heavy writers stay behind the existing dynamic imports, so nobody who never exports downloads them. src/services/activityExport.js(+test), README.md; 21 tests added (1420 pass). The four buttons are T-0116.
T-0116 | YES | 1.30 | All four surfaces that could only hand over a hand-rolled CSV now have the Export ▾ menu: the Activity Log header, its bulk-action bar (a person who picked eight rows to send on wants a document too), the WBS modal, the WBS page scoped activity log, and the per-project activity log in Projects. Each passes a build() that runs only when a format is picked, so opening a page or a modal costs nothing, and every hand-rolled CSV writer and its escaper is gone — the CSV is one of the menu entries, so nothing is lost. The acceptance criterion is tested against the real bytes: an .xlsx named activity-log-YYYY-MM-DD.xlsx loaded back with ExcelJS, with a bold frozen header row, the ten columns in order, the rows in the order they are on screen, and hours as numbers rather than text; the WBS one proves the phases come out in the project OWN order rather than alphabetically. The round-trip guard from T-0114 caught a real break while I was here: the new export names its comment column "What was done" (the wizard own label) and that was not among the import aliases, so the app could no longer re-import its own export — the alias was added and the guard now compares ACTIVITY_COLUMNS against the wizard mapping directly, which is a stronger check than reading a header row out of a component. src/components/{TableView,WBSView,WbsModal,ProjectsView}.jsx, src/services/csv.js, tests/ui/{activityExportUi,importWizard}.test.mjs, README.md; 14 tests added (1434 pass).
T-0117 | YES | 0.25 | Tests and docs for BUG-031. Thirty-nine tests cover it: 21 pure ones in src/services/activityExport.test.mjs (every column, the deleted-task snapshot, hours as numbers rather than NaN, day grouping newest-first, singular/plural in the subtitle, the empty-log file that still carries its header, the WBS keeping the project own phase order, an orphaned phase, the clamped arithmetic and the empty-group 0) and 18 in tests/ui/activityExportUi.test.mjs, which writes real .xlsx bytes and loads them back with ExcelJS to check the frozen bold header, the column order, the row order and the numeric hours — plus per-surface checks that all four pages offer the menu, none still hand-rolls a CSV, the bulk bar exports the selection rather than the whole log, and build() is lazy everywhere. A new repo-wide guard fails the build if any component grows a CSV escaper again. CLAUDE.md gained the module to the exports map and a pitfall carrying the round-trip lesson this row turned up: a column the app exports must be an alias the import wizard recognises, or the app cannot read its own file back. tests/ui/activityExportUi.test.mjs, CLAUDE.md, README.md, CHANGELOG.md; 4 tests added (1438 pass).
T-0118 | YES | 0.50 | New src/hooks/useActivate.js: activateProps(onActivate, { disabled, label }) returns every prop a div needs to really be a button. The ARIA button pattern is a contract — Enter AND Space both activate, and Space must not scroll instead — and the rows that carry role="button" were each made keyboard-reachable by hand, so most handled only Enter and a screen-reader user pressing the spacebar got a page scroll and no action. The helper handles Enter, Space and the older "Spacebar" spelling, preventDefault on both (Space scrolls, Enter submits a form), and stopPropagation because a row often sits inside something else clickable and the key press belongs to whatever has focus. A disabled row is still announced (aria-disabled) but is taken out of the tab order and does nothing on either key or a click, the same as a disabled button. The optional label is for a row whose visible text says nothing; a row that reads as a sentence deliberately gets none, since a label repeating the visible text is noise. src/hooks/useActivate.js, tests/ui/activateProps.test.mjs, README.md; 11 tests added (1449 pass). The call sites are T-0119.
T-0119 | YES | 0.70 | All six role="button" divs moved onto activateProps: the Dashboard action queue, its project-health rows and its next-five-days rows (all three of which handled only Enter, so Space scrolled the page and did nothing), the task editor subtask row and the Work Performed edit control (both already correct, moved for one implementation), and the sidebar saved-view delete — which had the confirm dialog written out TWICE, once for the click path and once for the key path, two copies free to drift apart, now one. Two rows gained a name they lacked: the project-health row ("Open Bridged on the board") and the bare ✕ delete ("Remove saved view …"); the queue row deliberately got none, since it already reads as a sentence and a label repeating visible text is noise. Two repo-wide guards, and the second one needed care: an Enter-only keydown is the BUG on a div pretending to be a button and the correct convention on a text input, where Enter submits and Space types a space — so the guard looks back for the owning tag and exempts input/textarea/select, with a companion test asserting that exemption actually matches something, or the guard would be unproven. src/components/{DashboardView,TaskEditor,WorkPerformedView,AppShell}.jsx, tests/ui/activateProps.test.mjs, CLAUDE.md; 6 tests added (1455 pass).
