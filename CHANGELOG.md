# Changelog

One line per task from the Ideon task sheet.

## 2026-09-20 — audit-and-task-2026-09-20

- T-0001 — Any user can self-assign a company and read its AI key — firestore.rules, src/services/firebase.js, tests/rules/harness.mjs, tests/rules/users.rules.test.mjs, package.json, firebase.json
- T-0002 — Invite links let any signed-in user become project admin (API/data layer) — firestore.rules, src/services/firebase.js, src/hooks/usePresence.js, src/components/TaskEditor.jsx, tests/rules/invites.rules.test.mjs, tests/rules/presence.rules.test.mjs
- T-0003 — Invite links let any signed-in user become project admin (UI) — src/services/access.js, src/services/access.test.mjs, src/components/ProjectsView.jsx, src/components/InviteClaimView.jsx, src/hooks/usePresence.js
- T-0004 — Invite links let any signed-in user become project admin (tests & docs) — README.md, tests/rules/invites.rules.test.mjs, tests/rules/presence.rules.test.mjs, src/services/access.test.mjs
- T-0005 — Approval gate is client-side only; pending users can write data — firestore.rules, src/App.jsx, tests/rules/approval.rules.test.mjs
- T-0006 — Next recurring task is created without workspaceId — src/services/recurrence.js, src/services/recurrence.test.mjs, src/services/firebase.js, src/hooks/useWorkspace.js
- T-0007 — Bridge lets web pages set the executable it spawns — bridge/ai.mjs, bridge/server.mjs, bridge/settings.test.mjs, src/services/ai.js, src/components/SettingsView.jsx, README.md
- T-0008 — No error boundary; render errors blank the app — src/components/ErrorBoundary.jsx, src/services/errorMessages.js, src/services/errorMessages.test.mjs, src/App.jsx, src/main.jsx, src/App.css, dev/error-boundary.html, dev/error-boundary.jsx, tests/ui/*.mjs, package.json
- T-0009 — Tests cover only bridge parsing and due-alert rules (API/data layer) — src/services/csv.js, src/services/csv.test.mjs, src/services/askAiCore.js, src/services/askAiCore.test.mjs, src/services/askAi.js, src/services/nlpQuickAdd.js, src/services/nlpQuickAdd.test.mjs
- T-0010 — Tests cover only bridge parsing and due-alert rules (UI) — src/components/CsvImporter.jsx, src/services/csv.js, src/services/csv.test.mjs, tests/ui/CsvImporter.test.mjs, tests/ui/dom.mjs, tests/ui/jsx-hooks.mjs
- T-0011 — Tests cover only bridge parsing and due-alert rules (tests & docs) — README.md, CLAUDE.md, CHANGELOG.md
- T-0012 — Default project setting is never applied to quick-add — src/components/TaskForm.jsx, src/services/preferences.js, src/services/preferences.test.mjs, tests/ui/TaskForm.test.mjs
- T-0013 — 'Shared' badge can never render — src/hooks/useTasks.js, src/services/projects.js, src/services/projects.test.mjs
- T-0014 — Export file dates use UTC, not the user's day (API/data layer) — src/services/download.js, src/services/download.test.mjs
- T-0015 — Export file dates use UTC, not the user's day (UI) — src/components/TableView.jsx, src/components/SettingsView.jsx, src/components/ProjectsView.jsx, src/components/WbsModal.jsx, src/components/WBSView.jsx, tests/ui/download.test.mjs
- T-0016 — Export file dates use UTC, not the user's day (tests & docs) — README.md, CLAUDE.md, CHANGELOG.md
- T-0017 — Bridge API mode is labelled as subscription billing (API/data layer) — src/services/ai.js, src/services/aiProvider.test.mjs
- T-0018 — Bridge API mode is labelled as subscription billing (UI) — src/components/SettingsView.jsx, tests/ui/AiBrainPanel.test.mjs
- T-0019 — Bridge API mode is labelled as subscription billing (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0020 — Sidebar user block polls auth every 500 ms — src/components/AppShell.jsx, tests/ui/SidebarUserBlock.test.mjs
- T-0021 — Docs are stale and there is no root README (API/data layer) — README.md, docs/archive/BUILD-GUIDE-2025.md, docs/archive/package-scripts-2025.md, docs/archive/README.md, docs/firestore-schema.md
- T-0022 — Docs are stale and there is no root README (UI) — src/components/SettingsView.jsx, src/components/CsvImporter.jsx, 12 other components, src/services/firebase.js, tests/ui/copy.test.mjs
- T-0023 — Docs are stale and there is no root README (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0024 — Local run needs two commands and hidden Firebase config — package.json, scripts/start.mjs, src/main.jsx, src/components/SetupRequiredView.jsx, src/services/firebaseConfig.js, src/services/firebaseConfig.test.mjs, src/App.css, README.md, CLAUDE.md, tests/ui/SetupRequiredView.test.mjs
- T-0025 — Overdue notification memory grows forever — src/services/dueAlerts.js, src/services/dueAlerts.test.mjs, src/hooks/useNotifications.js
- T-0026 — CLI install instructions shown to every user (API/data layer) — src/services/knowledgeCopy.js, src/services/knowledgeCopy.test.mjs
- T-0027 — CLI install instructions shown to every user (UI) — src/components/KnowledgeSection.jsx, src/components/SettingsView.jsx, tests/ui/KnowledgeSection.test.mjs
- T-0028 — CLI install instructions shown to every user (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0029 — Stale copy: anonymous session, email notification promise (API/data layer) — src/services/approvalCopy.js, src/services/approvalCopy.test.mjs
- T-0030 — Stale copy: anonymous session, email notification promise (UI) — src/components/PendingApprovalView.jsx, src/components/LandingView.jsx, src/components/SettingsView.jsx, tests/ui/approvalScreens.test.mjs
- T-0031 — Stale copy: anonymous session, email notification promise (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0032 — Package version and deploy account hardcoded — package.json, vite.config.js, scripts/firebase-deploy.mjs, src/services/appVersion.js, src/services/appVersion.test.mjs, src/components/SettingsView.jsx, src/components/AppShell.jsx, src/App.css, README.md
- T-0033 — Board cards each open their own workspace listeners — src/services/sharedSubscription.js, src/services/sharedSubscription.test.mjs, src/hooks/useWorkspace.js, src/hooks/useTasks.js, tests/ui/sharedListeners.test.mjs, bridge/settings.test.mjs
- T-0034 — Company Anthropic key is shipped to every member's browser (API/data layer) — functions/index.js, functions/src/authorize.js, functions/src/authorize.test.mjs, functions/package.json, firestore.rules, src/services/ai.js, src/services/aiCredentials.js, src/services/aiProxyClient.js, src/services/aiProxy.test.mjs, src/services/firebase.js, src/hooks/useCompany.js, firebase.json, package.json, scripts/firebase-deploy.mjs, tests/rules/companySecret.rules.test.mjs
- T-0035 — Company Anthropic key is shipped to every member's browser (UI) — src/components/SettingsView.jsx, tests/ui/companyKey.test.mjs
- T-0036 — Company Anthropic key is shipped to every member's browser (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0037 — Whole-collection subscriptions will not scale (API/data layer) — src/services/firebase.js, firestore.indexes.json, firebase.json, package.json, scripts/firebase-deploy.mjs, src/services/firestoreQueries.test.mjs
- T-0038 — Whole-collection subscriptions will not scale (UI) — src/hooks/useTasks.js, src/components/TableView.jsx, src/components/WorkPerformedView.jsx, src/App.css, tests/ui/activityPaging.test.mjs
- T-0039 — Whole-collection subscriptions will not scale (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0040 — Members are added by raw Firebase UID, not email (API/data layer) — src/services/invites.js, src/services/invites.test.mjs, src/services/firebase.js, src/hooks/useWorkspace.js, firestore.rules, tests/rules/workspaceInvite.rules.test.mjs
- T-0041 — Members are added by raw Firebase UID, not email (UI) — src/components/SettingsView.jsx, tests/ui/workspaceMembers.test.mjs
- T-0042 — Members are added by raw Firebase UID, not email (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0043 — Webhooks are stored but never delivered (API/data layer) — functions/webhooks.js, functions/index.js, functions/src/webhookEvents.js, functions/src/webhookEvents.test.mjs, functions/src/signature.test.mjs, firestore.rules, firestore.indexes.json, src/services/firebase.js
- T-0044 — Webhooks are stored but never delivered (UI) — src/components/SettingsView.jsx, src/App.css, tests/ui/webhookDelivery.test.mjs
- T-0045 — Webhooks are stored but never delivered (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0046 — No Word, Excel, PDF or Markdown exports (API/data layer) — src/services/exporters.js, src/services/exporters.test.mjs, tests/ui/exporters.test.mjs, package.json
- T-0047 — No Word, Excel, PDF or Markdown exports (UI) — src/components/ExportButton.jsx, src/services/taskExport.js, src/services/taskExport.test.mjs, src/services/minutesExport.js, src/services/minutesExport.test.mjs, src/services/exporters.js, src/components/ReviewView.jsx, src/components/Board.jsx, src/components/MinutesView.jsx, src/components/GoalsView.jsx, src/components/SettingsView.jsx, src/App.css, tests/ui/exportButtons.test.mjs
- T-0048 — No Word, Excel, PDF or Markdown exports (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0049 — Configurable table views with columns, group and sort (API/data layer) — src/services/tableViews.js, src/services/tableViews.test.mjs, src/services/firebase.js
- T-0050 — Configurable table views with columns, group and sort (UI) — src/components/TasksTableView.jsx, src/App.jsx, src/components/AppShell.jsx, src/App.css, tests/ui/tasksTable.test.mjs
- T-0051 — Configurable table views with columns, group and sort (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0052 — Long notebook text sources fail on argv size — bridge/notebooklm.mjs, bridge/notebooklm.test.mjs, CLAUDE.md
- T-0053 — Minutes cannot be exported or shared — src/components/MinutesView.jsx, src/App.css, tests/ui/minutesShare.test.mjs
- T-0054 — Task and project export is JSON only — src/components/GanttView.jsx, src/components/SettingsView.jsx, tests/ui/dataExport.test.mjs
- T-0055 — Quick create exists only for tasks — src/services/commandPalette.js, src/services/commandPalette.test.mjs, src/hooks/useQuickCreate.js, src/components/AppShell.jsx, src/components/Board.jsx, src/components/TaskForm.jsx, src/components/ProjectsView.jsx, src/components/MinutesView.jsx, src/components/GoalsView.jsx, src/App.css, tests/ui/commandPalette.test.mjs
- T-0056 — Projects and segments have colors but no icons — src/services/icons.js, src/services/icons.test.mjs, src/components/ProjectsView.jsx, src/services/firebase.js, src/App.css, tests/ui/groupIcons.test.mjs
- T-0057 — Soft-deleted items cannot be seen or restored — src/components/TrashView.jsx, src/services/firebase.js, src/App.jsx, src/components/AppShell.jsx, tests/ui/trash.test.mjs
- T-0058 — Weekly timesheet per person exportable to Excel — src/services/timesheet.js, src/services/timesheet.test.mjs, src/components/TimesheetView.jsx, src/App.jsx, src/components/AppShell.jsx, src/services/commandPalette.js, src/App.css, tests/ui/timesheet.test.mjs
- T-0059 — Import wizard for tasks and projects — src/services/csv.js, src/services/csv.test.mjs, src/components/ImportWizard.jsx, src/components/TableView.jsx, src/components/TasksTableView.jsx, src/App.css, tests/ui/importWizard.test.mjs
- T-0060 — Process template gallery — src/templates/gallery.js, src/templates/gallery.test.mjs, src/components/TemplateGallery.jsx, src/components/ProjectsView.jsx, src/App.css, package.json, tests/ui/templateGallery.test.mjs
- T-0061 — Undo toast and Trash view — src/components/Toast.jsx, src/App.jsx, src/App.css, src/components/TaskEditor.jsx, src/components/ProjectsView.jsx, src/components/MinutesView.jsx, tests/ui/toast.test.mjs
- T-0062 — Command palette with create and navigate actions — src/services/commandPalette.js, src/services/commandPalette.test.mjs, src/components/AppShell.jsx, tests/ui/commandPalette.test.mjs
- T-0063 — Portfolio status report as PDF — src/services/statusReport.js, src/services/statusReport.test.mjs, src/components/DashboardView.jsx, tests/ui/statusReport.test.mjs
- T-0064 — 95 native alert/prompt/confirm dialogs (API/data layer) — src/components/Dialog.jsx, src/App.jsx, src/App.css, tests/ui/dialog.test.mjs, tests/ui/dom.mjs
- T-0065 — 95 native alert/prompt/confirm dialogs (UI) — 21 components across src/components/, tests/ui/noNativeDialogs.test.mjs
- T-0066 — 95 native alert/prompt/confirm dialogs (tests & docs) — CLAUDE.md, README.md, CHANGELOG.md
- T-0067 — Modals lack dialog semantics, focus trap and Esc (API/data layer) — src/hooks/useModalDialog.js, tests/ui/useModalDialog.test.mjs
- T-0068 — Modals lack dialog semantics, focus trap and Esc (UI) — 21 components across src/components/, tests/ui/modalSemantics.test.mjs
- T-0069 — Modals lack dialog semantics, focus trap and Esc (tests & docs) — 10 components across src/components/, CLAUDE.md, README.md, tests/ui/modalSemantics.test.mjs
- T-0070 — Automation rules replacing dead webhooks (API/data layer) — functions/automations.js, functions/index.js, functions/src/automations.js, functions/src/automations.test.mjs, firestore.rules, firestore.indexes.json, tests/rules/automations.rules.test.mjs
- T-0071 — Automation rules replacing dead webhooks (UI) — src/components/AutomationsSection.jsx, src/components/SettingsView.jsx, src/services/firebase.js, src/App.css, functions/src/automations.js, dev/automations.html, dev/automations.jsx, tests/ui/automations.test.mjs
- T-0072 — Automation rules replacing dead webhooks (tests & docs) — functions/src/automationFlow.test.mjs, README.md, CLAUDE.md, CHANGELOG.md
- T-0073 — Attachments uploaded by one member cannot be deleted by others — src/services/uploadPaths.js, storage.rules, src/services/firebase.js, src/components/FileUpload.jsx, src/components/ActivityLogger.jsx, src/components/ActivityEditor.jsx, src/components/WorkspaceEditor.jsx, firebase.json, package.json, tests/rules/attachments.rules.test.mjs, tests/ui/attachments.test.mjs, README.md, CLAUDE.md
- T-0074 — Custom fields are invisible outside the task editor — src/services/customFields.js, src/services/tableViews.js, src/services/taskExport.js, src/components/Board.jsx, src/components/GanttView.jsx, src/components/TasksTableView.jsx, src/App.css, tests/ui/customFields.test.mjs, README.md, CLAUDE.md
- T-0075 — Mentions and a personal inbox (API/data layer) — src/services/mentions.js, src/services/firebase.js, firestore.rules, functions/automations.js, tests/rules/notifications.rules.test.mjs, tests/ui/inboxApi.test.mjs, README.md, CLAUDE.md
- T-0076 — Mentions and a personal inbox (UI) — src/components/InboxBell.jsx, src/components/InboxPanel.jsx, src/components/Markdown.jsx, src/components/TaskEditor.jsx, src/components/AppShell.jsx, src/components/Board.jsx, src/components/AutomationsSection.jsx, src/hooks/useInbox.js, src/services/openTask.js, src/services/mentions.js, src/App.css, dev/inbox.html, dev/inbox.jsx, tests/ui/inbox.test.mjs, README.md, CLAUDE.md
- T-0077 — Mentions and a personal inbox (tests & docs) — tests/ui/inboxFlow.test.mjs, README.md, CHANGELOG.md
- T-0078 — Workload planner by week (API/data layer) — src/services/workload.js, src/services/firebase.js, tests/ui/workloadApi.test.mjs, README.md, CLAUDE.md
- T-0079 — Workload planner by week (UI) — src/components/WorkloadView.jsx, src/services/workload.js, src/App.jsx, src/components/AppShell.jsx, src/App.css, dev/workload.html, dev/workload.jsx, tests/ui/workload.test.mjs, README.md, CLAUDE.md
- T-0080 — Workload planner by week (tests & docs) — tests/ui/workloadFlow.test.mjs, README.md, CHANGELOG.md
- T-0081 — Read-only share links for a board or Gantt (API/data layer) — src/services/shareLinks.js, firestore.rules, src/services/firebase.js, tests/rules/sharedViews.rules.test.mjs, tests/ui/shareLinksApi.test.mjs, README.md, CLAUDE.md
- T-0082 — Read-only share links for a board or Gantt (UI) — src/components/SharedViewPage.jsx, src/components/SharedSnapshot.jsx, src/components/ShareLinksPanel.jsx, src/components/ProjectsView.jsx, src/App.jsx, src/services/shareLinks.js, src/App.css, dev/shared.html, dev/shared.jsx, tests/ui/shareLinks.test.mjs, README.md, CLAUDE.md
- T-0083 — Read-only share links for a board or Gantt (tests & docs) — tests/ui/shareFlow.test.mjs, README.md, CHANGELOG.md
- T-0084 — Scheduled recurrence instead of on-completion only — src/services/recurrenceSchedule.js, src/services/firebase.js, src/hooks/useRecurrenceCatchUp.js, src/App.jsx, dev/recurrence.html, dev/recurrence.jsx, tests/ui/recurrenceCatchUp.test.mjs, README.md, CLAUDE.md

- **T-0085 — Timer widget swallows Escape for the whole app (API/data layer)** — `useModalDialog` takes an `open` flag (default `true`); when it is `false` the effect does nothing at all, and the Escape branch now returns early unless the panel is really in the document. `src/hooks/useModalDialog.js`, `tests/ui/useModalDialog.test.mjs`.
- **T-0086 — Timer widget swallows Escape for the whole app (UI)** — `TimerWidget` passes `open: confirmOpen` to `useModalDialog`, so the ⌘K dropdown, the Export ▾ menu, the inbox panel, the Task-table column picker, the tutorial tour and the due-task alert all hear Escape again. New `dev/escape-key.html` harness. `src/components/TimerWidget.jsx`, `dev/escape-key.{html,jsx}`, `tests/ui/escapeKey.test.mjs`.
- **T-0087 — Timer widget swallows Escape for the whole app (tests & docs)** — 12 tests across `tests/ui/useModalDialog.test.mjs` and `tests/ui/escapeKey.test.mjs`, plus the pitfall in `CLAUDE.md` and the suite/harness rows in `README.md`.
- **T-0088 — Recurring task done from the editor spawns no next instance** — the spawn gate moved into the pure `services/recurrence.js` (`shouldSpawnRecurrence`, `alreadySpawned`) behind one exported `maybeSpawnRecurrence`, which `setTaskStatus` and the task editor's Save both call. `src/services/recurrence.js`, `src/services/firebase.js`, `src/components/TaskEditor.jsx`, `tests/ui/recurrenceOnSave.test.mjs`.
- **T-0089 — Gantt draws an empty row for a task with only a due date (API/data layer)** — new pure `src/services/ganttGeometry.js`: a one-date plan is a one-day milestone that draws and can be dragged. `src/services/ganttGeometry.js`, `src/services/ganttGeometry.test.mjs`.
- **T-0090 — Gantt draws an empty row for a task with only a due date (UI)** — `GanttRow` renders and drags through `services/ganttGeometry`; a one-date plan is a visible milestone whose left edge can be dragged into a real range. New `dev/gantt.html` harness. `src/components/GanttView.jsx`, `src/App.css`, `dev/gantt.{html,jsx}`, `tests/ui/ganttMilestone.test.mjs`.
- **T-0091 — Gantt draws an empty row for a task with only a due date (tests & docs)** — 38 tests across `src/services/ganttGeometry.test.mjs` and `tests/ui/ganttMilestone.test.mjs`, plus the pitfall in `CLAUDE.md` and the README rows.
- **T-0092 — Imported Status column is mapped, previewed, then thrown away** — new pure `services/taskStatus.js` (`statusStamps`) shared by `addTask` and `setTaskStatus`; `addTask` takes an optional status defaulting to `'todo'`; the import payload moved beside `IMPORT_KINDS` as `importedTaskPayload()`. `src/services/taskStatus.js`, `src/services/csv.js`, `src/services/firebase.js`, `src/components/ImportWizard.jsx`, `tests/ui/importStatus.test.mjs`.
- **T-0093 — Command palette's 'Log an activity' leads nowhere** — `WorkPerformedView` listens with `useQuickCreate('activity', …)`; `CREATE_VIEW` moved beside `CREATE_ORDER` so the commands and their destinations are one list, guarded by a repo-wide test. `src/components/WorkPerformedView.jsx`, `src/services/commandPalette.js`, `src/components/AppShell.jsx`, `tests/ui/quickCreateActivity.test.mjs`.
- **T-0094 — Raw AI error text is rendered straight to the user (API/data layer)** — `ai.js` errors carry a plain sentence as their message and the operator's version on `detail`; new `describeAiFailure()` / `isPlainUserMessage()` in `services/errorMessages.js`. `src/services/ai.js`, `src/services/errorMessages.js`.
- **T-0095 — Raw AI error text is rendered straight to the user (UI)** — all nine AI error sites go through `describeAiFailure()`; new `copy.test.mjs` guards on `setError(err.message)` and `{error.message}`; `signInWithGoogle` stops returning the SDK message and the Firebase-console instruction; `isOperatorProfile()` / `useIsOperator()` replace the hand-written role check. 9 components, `src/services/firebase.js`, `src/hooks/useUserProfile.js`, `tests/ui/aiErrorCopy.test.mjs`.
- **T-0096 — Raw AI error text is rendered straight to the user (tests & docs)** — 38 tests across `src/services/errorMessages.test.mjs`, `tests/ui/aiErrorCopy.test.mjs` and `tests/ui/copy.test.mjs`, plus two pitfalls in `CLAUDE.md`.
- **T-0098 — Modal focus is stolen on every parent re-render** — `useModalDialog` keeps `onClose` / `closeOnEscape` / `autoFocus` in a ref and keys its effect on `[open]` alone, so focus is moved in once per open. `src/hooks/useModalDialog.js`, `tests/ui/useModalDialog.test.mjs`.
- **T-0099 — Quick create drops the name for project, minutes and goal (API/data layer)** — `newSeed()` and `useSeededField()` in `hooks/useQuickCreate.js` hold the seed shape and the apply-once rule; Board and TaskForm moved onto them. `src/hooks/useQuickCreate.js`, `src/components/{Board,TaskForm}.jsx`, `tests/ui/quickCreateSeed.test.mjs`.
- **T-0100 — Quick create drops the name for project, minutes and goal (UI)** — the three pages carry the typed text into their editors via `useSeededField`; only a new one is seeded, an empty ask stays blank, and closing clears it. `src/components/{ProjectsView,MinutesView,GoalsView}.jsx`, `tests/ui/quickCreateSeed.test.mjs`.
- **T-0101 — Quick create drops the name for project, minutes and goal (tests & docs)** — 23 tests in `tests/ui/quickCreateSeed.test.mjs` plus the pitfall in `CLAUDE.md`.
- **T-0102 — Saved view's tag filter is ignored by Gantt and Activity Log (API/data layer)** — new pure `src/services/tagFilter.js` (`tagsOf`, `filterByTag`, `availableTags`, `tagFilterState`), including where an activity's tags come from; Board moved onto it. `src/services/tagFilter.js`, `src/components/Board.jsx`.
- **T-0103 — Saved view's tag filter is ignored by Gantt and Activity Log (UI)** — Gantt, Activity Log and Calendar declare and apply `initialTagFilter`; new shared `TagFilterBar` replaces Board's hand-rolled strip; written empty states; new `dev/tag-filter.html`. `src/components/{TagFilterBar,Board,GanttView,TableView,CalendarView}.jsx`, `tests/ui/tagFilterViews.test.mjs`.
- **T-0104 — Saved view's tag filter is ignored by Gantt and Activity Log (tests & docs)** — 29 tests across `src/services/tagFilter.test.mjs` and `tests/ui/tagFilterViews.test.mjs`, plus the pitfall in `CLAUDE.md`.
- **T-0105 — Notification test reports success as an error, with a debug trail** — the trail goes to `console.debug`, the user gets one plain sentence, green on success; the OS advice moved under the permission badge. `src/components/SettingsView.jsx`, `tests/ui/copy.test.mjs`.
- **T-0106 — Account page tells users to share an ID nobody can use** — the Account ID box is superadmin-only and behind a Technical details disclosure; everyone else is told to ask for an email invite. `src/components/SettingsView.jsx`, `tests/ui/copy.test.mjs`.
- **T-0107 — Due-alert switch says off while OS notifications keep firing** — `useOverdueScan` is gated on `prefs.enabled`; the switch label and the Settings copy say the switch covers the desktop too. `src/hooks/useNotifications.js`, `src/components/DueAlertBell.jsx`, `src/components/SettingsView.jsx`, `CLAUDE.md`.
- **T-0108 — Two buttons named Log on every board card** — the history toggle is now "History · n" / "Hide history" with `aria-expanded` and matching labels; `+ Log` is the only control called Log. `src/components/Board.jsx`, `tests/ui/copy.test.mjs`.
- **T-0109 — Unknown view falls back to the Ask AI title** — the topbar falls back to the app name; new `NotFoundView` with a way back to the Dashboard; `RENDERABLE_VIEWS` / `isKnownView()` exported and guarded in both directions. `src/components/AppShell.jsx`, `src/components/NotFoundView.jsx`, `src/App.jsx`, `tests/ui/unknownView.test.mjs`.
- **T-0110 — addTask silently drops status, progress and actual dates** — `status`, `progress` and `actual` are all optional and honoured; `clampProgress()` distinguishes an explicit 0 from no statement and clamps out-of-range values. `src/services/taskStatus.js`, `src/services/firebase.js`, `CLAUDE.md`.
- **T-0111 — Settings polls the notification permission twice a second** — new shared `useNotificationPermission()` (Permissions API `change` + a window `focus` fallback) replaces the 1.5 s interval; `useOverdueScan` uses it too. `src/hooks/useNotifications.js`, `src/components/SettingsView.jsx`, `tests/ui/notificationPermission.test.mjs`.
- **T-0112 — Bridge failure copy hands shell commands to ordinary users** — `mockReply`'s body is plain; the runbook travels as `operatorHint` and renders through the new `<AiOperatorHint>` only for an operator; the degraded sentences stop naming the CLI and the provider id. `src/services/ai.js`, `src/components/{AiOperatorHint,DueTaskAlertModal}.jsx`, `tests/ui/aiOperatorCopy.test.mjs`.
- **T-0113 — Task table, Trash, Workload and Artifacts are unreachable from ⌘K** — new `src/services/views.js` registry feeds the sidebar, the topbar, the not-found check and the palette; `NAV_TARGETS` is derived and every page carries the words people reach for. `src/services/views.js`, `src/services/commandPalette.js`, `src/components/AppShell.jsx`, `tests/ui/commandPalette.test.mjs`.
- **T-0114 — Two competing import buttons on the Activity Log** — one "Import" button opening the wizard; `CsvImporter` retired after proving the wizard auto-maps a Task Monitor export, its tests migrated. `src/components/TableView.jsx`, `src/components/CsvImporter.jsx` (removed), `tests/ui/importWizard.test.mjs`.
- **T-0115 — Activity Log and WBS can only produce a CSV (API/data layer)** — new pure `src/services/activityExport.js` with `buildActivityLogDocument()` and `buildWbsDocument()`. `src/services/activityExport.js`, `src/services/activityExport.test.mjs`.
- **T-0116 — Activity Log and WBS can only produce a CSV (UI)** — Export ▾ on the Activity Log (header and bulk bar), the WBS modal, the WBS activity log and the per-project activity log; the hand-rolled CSV writers are gone and "What was done" became an import alias so the app can re-import its own export. `src/components/{TableView,WBSView,WbsModal,ProjectsView}.jsx`, `src/services/csv.js`, `tests/ui/activityExportUi.test.mjs`.
- **T-0117 — Activity Log and WBS can only produce a CSV (tests & docs)** — 39 tests across `src/services/activityExport.test.mjs` and `tests/ui/activityExportUi.test.mjs`, plus the pitfall in `CLAUDE.md`.
- **T-0118 — Clickable rows respond to Enter but not Space (API/data layer)** — new `src/hooks/useActivate.js` (`activateProps`) implements the ARIA button contract in one place. `src/hooks/useActivate.js`, `tests/ui/activateProps.test.mjs`.
- **T-0119 — Clickable rows respond to Enter but not Space (UI)** — all six `role="button"` divs spread `activateProps()`; two gained accessible names; the saved-view delete's duplicated confirm became one. `src/components/{DashboardView,TaskEditor,WorkPerformedView,AppShell}.jsx`, `tests/ui/activateProps.test.mjs`, `CLAUDE.md`.
- **T-0120 — Clickable rows respond to Enter but not Space (tests & docs)** — 20 tests in `tests/ui/activateProps.test.mjs` plus the pitfall in `CLAUDE.md`.
- **T-0121 — Column reorder buttons say left, show up** — the Task table's column picker draws `←` / `→` to match the names "Move <column> left/right" and gained matching `title`s; the modal-semantics guard now compares the direction word in an `aria-label` against the arrow the button draws, reading the whole interpolated attribute (the first regex stopped at `${col` and skipped the bug it was written for) and failing if it finds no arrow buttons at all. `src/components/TasksTableView.jsx`, `tests/ui/modalSemantics.test.mjs`.
- **T-0122 — Dashboard 'Log time' opens a read-only list** — the hero button opens `ActivityLogger` on a task it names on its face; the pick moved to the pure `src/services/logTime.js` (overdue → due today → in progress, and *ask* rather than fall through to `filtered[0]`); the flow moved to `LogTimeButton.jsx`; `LogActivityPicker` was lifted out of `WorkPerformedView` so both pages share one; `ActivityLogger` gained an inline "Change task" select that keeps what was already typed. `src/services/logTime.js`, `src/components/{LogTimeButton,LogActivityPicker,ActivityLogger,DashboardView,WorkPerformedView}.jsx`, `dev/log-time.{html,jsx}`, `tests/ui/logTime.test.mjs`.
- **T-0123 — Board cards never show a due date** — a due chip on the card meta row ("Due today" / "Due Fri" / "Due Oct 12" / "3d late"), replacing the bare "Overdue" badge; the rule is the pure `src/services/dueChip.js`, and done cards keep their early/late badge instead. `src/services/dueChip.js`, `src/components/Board.jsx`, `src/App.css`, `dev/due-chip.{html,jsx}`, `tests/ui/dueChip.test.mjs`.
- **T-0124 — Recurrence help text describes only half the feature** — the note names both the on-completion path and the catch-up path, plus the limit that the catch-up runs in the app; guarded as source text and as rendered output. `src/components/TaskEditor.jsx`, `tests/ui/copy.test.mjs`, `tests/ui/recurrenceNote.test.mjs`, `CLAUDE.md`.
- **T-0125 — AI-unavailable message blames the wrong thing** — new `aiUnavailableCopy(status, { isOperator })` in `src/services/ai.js` gives the AI helper's empty state the real reason (company gate vs. nothing connected, via `providerHeadline`), and moves the shell command and the Settings section behind `AiOperatorHint`. `src/services/ai.js`, `src/components/AiHelper.jsx`, `dev/ai-off.{html,jsx}`, `tests/ui/{AiBrainPanel,aiHelperEmpty}.test.mjs`.
- **T-0126 — Default theme ignores the operating system's dark mode** — `DEFAULTS.theme` is `'system'`; `color-scheme` declared beside the tokens in all three states so native chrome follows the theme; the Appearance note dropped the CSS property name. `src/hooks/useSettings.js`, `src/App.css`, `src/components/SettingsView.jsx`, `tests/ui/themeDefault.test.mjs`.
- **T-0127 — One bulk-action bar exists, and it is on the wrong table (API/data layer)** — new pure `src/services/bulkTasks.js` (six actions, per-task patch, inverse patch for Undo, 400-write chunking, shift/⌘ selection maths, plain-language sentences) and `bulkUpdateTasks()` in `firebase.js` committing them in series. `src/services/bulkTasks.js`, `src/services/firebase.js`, `tests/ui/bulkTasksApi.test.mjs`, `tests/rules/bulkTasks.rules.test.mjs`.
- **T-0128 — One bulk-action bar exists, and it is on the wrong table (UI)** — a selection column, select-all, shift-range and ⌘-click on the task table, plus a bulk bar rendered from `BULK_ACTIONS` with plain-language pickers, a confirm on delete and a toast with Undo; the grid was extracted as `TasksTableGrid` so it can be mounted and clicked in tests. `src/components/TasksTableView.jsx`, `src/App.css`, `dev/bulk-tasks.{html,jsx}`, `tests/ui/bulkTasksUi.test.mjs`.
- **T-0129 — One bulk-action bar exists, and it is on the wrong table (tests & docs)** — the sequencing moved to `src/hooks/useBulkTasks.js` so the whole acceptance path can be driven by a test; `tests/ui/bulkTasksFlow.test.mjs` covers it end to end and caught the partial-failure count being discarded by `friendlyError`. README gains "Changing many tasks at once"; CLAUDE.md gains the hook and the pitfalls. `src/hooks/useBulkTasks.js`, `src/components/TasksTableView.jsx`, `tests/ui/bulkTasksFlow.test.mjs`, `README.md`, `CLAUDE.md`.
