# Task Monitor — Claude Code executor prompt

_Generated by Ideon on 2026-09-21 for release v0.2. Paste the block below into Claude Code._

## How to run this

### Before the first run (one-time setup)

- Node.js is installed: in a terminal, `node -v` prints a version (22 or 24). If not, install it from nodejs.org.
- Claude Code is installed and signed in: run `npm install -g @anthropic-ai/claude-code`, then type `claude` once and follow the sign-in link with your Claude account. Type `/exit` to leave.
- Your app runs: you can start task-monitor yourself the way you normally do, and it works before Claude Code touches anything.

### Steps (once per iteration)

1. Download the executor pack from Ideon → Execute and unzip it. Move its files into the app folder, next to package.json: 02-audit-2026-09-21.md, audit-and-task-2026-09-21.xlsx, task-status.md and this prompt file.
2. Open a terminal in the app folder and start Claude Code there: `cd /Users/aldenrayejorango/Developer/PROJECTS/task-monitor && claude`. Run `ls` first — it should list package.json and the files above.
3. Start Claude Code in that terminal: type `claude` and press Enter. The first time in a folder it asks whether you trust the folder — answer Yes.
4. Copy the whole prompt block below, paste it into the Claude Code box and press Enter. That is the only thing you type.
5. Let it work. It goes down the task sheet top to bottom: implements a row, runs the tests, estimates that row's AI cost, appends one YES (or BLOCKED with a reason) line plus its cost estimate to task-status.md, adds a CHANGELOG line and commits. Answer Yes when it asks permission to run a command or edit a file. It skips rows whose column R is On-Hold (T-0097, T-0133, T-0135, T-0136, T-0153, T-0154, T-0159, T-0160, T-0163, T-0164); release one by setting it to Proceed in Ideon → Tasks and exporting the pack again. Open task-status.md at any time to watch progress live. If it stops early or your computer sleeps, paste the same prompt again — logged rows are not redone.
6. When it prints the totals (done, blocked, on-hold), start the app the way you normally do and test what changed (Checkpoint B). Then go to Ideon → Tasks, press "Sync from Claude Code" and pick task-status.md from the app folder — every row it finished is marked here in one go. Then go to Ideon → Iterate and paste what you found, or name one section to sweep.

### If something goes wrong

- "Cannot find 02-audit-2026-09-21.md" or the xlsx: the files are not next to package.json, or the pack was downloaded on a different day than this prompt. Move the files, or generate the prompt again and download a fresh pack so the dates match.
- Claude Code says you are not signed in: type `/exit`, run `claude` on its own, complete the sign-in, then start again from step 3.
- A row came back BLOCKED: read the reason on its line in task-status.md (or column O of the Excel file), then paste your decision into Ideon → Iterate — the next audit turns it into a new row.
- The run ended but the Excel file still looks untouched: that is only the final sync step failing — task-status.md has every result. Upload that file in Ideon → Tasks → "Sync from Claude Code" instead.
- You want to undo a task: every task is its own git commit named "T-00xx: title", so a developer can revert one without touching the others.

## The prompt

```
You are the implementing engineer for Task Monitor. Work inside this repository (the existing app at /Users/aldenrayejorango/Developer/PROJECTS/task-monitor).

REFERENCE FILES (read fully before doing anything):
- ./02-audit-2026-09-21.md      — audit findings behind every task
- ./audit-and-task-2026-09-21.xlsx, sheet "Tasks" — the task queue (167 rows, 83 not yet done, 70 eligible for the loop below, 10 On-Hold: T-0097, T-0133, T-0135, T-0136, T-0153, T-0154, T-0159, T-0160, T-0163, T-0164)
- ./task-status.md   — append-only progress log; the record of what is already finished

STANDING REQUIREMENTS (apply to every change):
1. Non-savvy users must never see JSON, {{templates}} or code — inputs are plain-language text fields, dropdowns and toggles.
2. Every button and every "succeeding" action behind it must work end-to-end; no dead UI.
3. The app must be able to generate output files (.docx, .xlsx, .pptx, .md, .txt, .html, .pdf) where relevant.
4. Icons per group/category; a Grok-like "quick create" path for the main object of the app.
5. Runs locally on a MacBook via VS Code with one install command and one run command.
6. Every deliverable file is date-stamped in its name: <name>-YYYY-MM-DD.<ext>.

COLUMNS (A–S) — A Task ID · B Source ID · C Release · D Priority · E Category · F Area · G Task title · H Detailed instructions · I Acceptance criteria · J Files / modules · K Dependencies · L Estimate · M Test instructions · N Done? · O Notes · P Location (page / window / modal / panel) · Q Risk (Low / Medium / High) · R Recommendation (Proceed / On-Hold) · S Est. AI cost (USD, you fill this in per row).

BEFORE THE FIRST ROW — read ./task-status.md (create it with a "# Task status" heading if it is not there).
A row is already finished if task-status.md has a YES line for it, or column N is "YES". Never redo a finished row.

LOOP — repeat until no eligible row remains:
1. Open the Tasks sheet. Find the first row (top to bottom) that is not already finished (see above),
   whose column R ("Recommendation") is NOT "On-Hold", and every Task ID listed in column K ("Dependencies") is finished.
   A Task ID in column K that does not appear anywhere in column A counts as finished — that row was removed.
   On-Hold rows are waiting for a human decision: never implement, edit or skip-mark them — leave them and move past.
   If a dependency in column K is BLOCKED, this row cannot be built either: do not attempt it, and do not leave it
   silent. Log it straight away as "<Task ID> | BLOCKED | 0.00 | waiting on <dependency Task ID> — <that row's reason>"
   and move to the next row. Every row must end the run either finished or logged with a reason; a row that is
   waiting on a blocked row is the one case where you log BLOCKED without having tried to implement it.
2. Read columns G, H, I, J, M, P, Q for that row. Re-read the matching Source ID (column B) in 02-audit-2026-09-21.md for context.
3. Implement it completely. Rules:
   - Root-cause fixes, never patches. No TODOs, no placeholders, no "coming soon".
   - Non-savvy users never see JSON/templates/code — plain-language inputs only.
   - Every new button's succeeding actions must work end-to-end.
   - Add/extend automated tests per column M; run the full test suite (npm test); fix anything you broke.
   - Run npm run dev (or the project's dev command), open the page / window / modal / panel named in column P,
     and verify the acceptance criteria in column I. Respect column Q: a "High" risk row gets its test written before the change, not after.
4. ESTIMATE THE AI COST — before logging, estimate the AI cost (USD) of the work you just did on this row. Use the
   published Claude API pricing per million tokens (Haiku: $1 input / $5 output · Fable: $10 input / $50 output · Opus: $5 input / $25 output · Sonnet 4.x: $3 input / $15 output · Sonnet: $2 input / $10 output —
   pick the rate for the model you are running as) against your best approximation of the input tokens you read (the
   audit entry, the source files, this prompt's context) and the output tokens you produced (code, tests, commit
   message) for this row alone. This is an estimate, not a metered figure — round to 2 decimals (e.g. 0.42) and never
   leave it blank; use 0.00 only if you truly did no model work for the row.
5. LOG IT NOW — the moment the row's tests pass, before the changelog, before the commit, before you read the next row.
   One shell append, never a rewrite of the file:
     echo 'T-0007 | YES | 0.42 | Added renewal reminders; server/src/routes/reminders.ts, client/src/pages/Renewals.tsx; tests added.' >> task-status.md
   The third field is your cost estimate from step 4 (a plain number, no "$", no thousands separator).
   Write BLOCKED instead of YES if the row is impossible or wrong, with the reason as the note — never skip a row silently.
   A row held up by a blocked dependency uses the wording from step 1, so the human can see at a glance what unblocks it.
   Keep it to a single line with no line breaks. Do NOT batch these up, and do NOT leave them until the end of the run:
   this file is the only thing that survives a crash, a sleeping laptop, a cancelled run or a context reset.
   Do not touch the .xlsx during the loop — it is rewritten once at the end, from this log.
6. Append an entry to CHANGELOG.md: <Task ID> — <title> — <files>.
7. Commit (include task-status.md) with message "<Task ID>: <title>" and continue to the next eligible row.

AFTER THE LOOP — once, in this order:
a. Sync the workbook from the log: a small Node script with the "exceljs" package (already a dependency of the server
   workspace) — load audit-and-task-2026-09-21.xlsx, and for every line in task-status.md write column N (YES/BLOCKED), column O
   (the note) and column S (the cost estimate, as a number) on the row whose column A matches that Task ID. Save to
   the same path; do not create a copy. If this step fails for any reason, say so plainly — task-status.md still holds
   every result and nothing is lost.
b. If the project keeps its own plan or architecture notes, update them with what you learned and say what changed.
c. Print: total done, total blocked (with IDs), total On-Hold (with IDs — these need my review), the total estimated
   AI cost across every row you logged this run, the run/test commands for me to verify, and this reminder: upload
   task-status.md in Ideon → Tasks → "Sync from Claude Code" to mark the same rows there.
```

### After the run

> Run `npm run dev`, test the app, then upload task-status.md in Ideon → Tasks → "Sync from Claude Code" and come back to Ideon → Iterate to start the next iteration. Rows that are already YES keep their state.

Task sheet snapshot: 167 rows · 84 done · 0 blocked · 83 open · 10 On-Hold (change column R to Proceed in Ideon → Tasks to release them) · $119.15 estimated AI cost logged so far (column S).
